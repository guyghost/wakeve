# App Store Readiness - Wakeve

Date: 2026-06-20

## Apple Source Baseline

Apple-source review date: 2026-05-28.

- Apple says App Store Connect uploads must meet current SDK requirements; since 2026-04-28, iOS and iPadOS apps must be built with Xcode 26 or later and the iOS and iPadOS 26 SDK or later.
- Apple says builds must be uploaded and processed before they can be selected for an App Store version.
- Apple says before submitting an app version for review, required metadata must be provided and the build must be chosen for the version.
- Apple says the required role to submit an app for review is Account Holder, Admin, or App Manager.
- Apple says Add for Review changes the app status to Ready for Review, but the app is not sent to App Review until Submit for Review is clicked.
- Apple says App Review submissions should be final versions with all necessary metadata and fully functional URLs.
- Apple says placeholder text, empty websites, and temporary content should be removed before submission.
- Apple says apps should be tested on device for crashes, bugs, and stability before submission.
- Apple says account-based features require full review access through an active demo account or fully featured demo mode, plus needed resources.
- Apple says backend services must be live and accessible during review.
- Apple says apps with user-generated content must include filtering, reporting, blocking, timely moderation responses, and published contact information.
- Apple says apps that support account creation must let users initiate account deletion from inside the app.
- Apple says App Privacy answers are required for App Store distribution and must accurately describe app and third-party partner data practices.
- Apple says in-app purchase is required for digital content/features unlocked in the app unless an exception applies, while other payment methods can be used for physical goods or services consumed outside the app.
- Apple says app availability must be configured before App Review submission.
- Apple says EU Digital Services Act trader status information is required for applicable EU distribution workflows.
- Apple says app statuses include Prepare for Submission, Ready for Review, In Review, Binary Rejected, Waiting for Export Compliance, Pending Developer Release, and Ready for Distribution.

## Status

Wakeve is not ready for App Store submission yet. The iOS app compiles in Debug simulator and Release device mode without code signing, Fastlane metadata validation runs locally, the app icon asset catalog is populated, APNs/Siri/Sign in with Apple/Associated Domains entitlements are configured in the Xcode project with local unsigned capability-scan evidence, iPhone plus iPad screenshots are present in iOS metadata and in Fastlane's uploadable screenshots folder, App Review notes now describe a release-visible local-only guest path, and deployable web routes exist for the legal/support URLs plus both Apple App Site Association endpoints used by App Store Connect and Universal Links. Local Apple account placeholder gates are documented for Apple ID, App Store Connect team ID, Apple Developer Team ID, and AASA Team ID. Local third-party license inventory and notice artifacts have been regenerated and hashed for AS-21. Local pricing evidence supports a first-release free/no-IAP App Store configuration, while App Store Connect pricing and storefront evidence remains required. The local toolchain is Xcode 27.0 with iOS SDK 27.0, which satisfies Apple's current iOS/iPadOS SDK upload minimum. The built Release app declares standard export compliance, only When In Use location access, remote-notification background mode, no legacy armv7 device capability, Bundle ID `com.guyghost.wakeve`, display name `Wakeve`, marketing version `1.0`, build version `1`, the `wakeve://` URL scheme, Siri and speech recognition usage text, camera/photo/location purpose strings, no localhost/cleartext HTTP strings in the app or shared framework binaries, no IDFA/App Tracking Transparency strings in the app or shared framework binaries, no active iOS launch-source `TODO`/`FIXME` markers, no raw Swift `print`/`debugPrint`/`NSLog` launch-source logging outside the DEBUG-only logger, no unconfigured BackgroundTasks symbols in the Release binary, and a non-empty privacy manifest aligned with the current privacy-label draft. The local Release dSYM UUID matches the app executable UUID for the unsigned build. The web app also passes Svelte type checking and production build. `api.wakeve.app` now resolves publicly and the live health URL is reachable through Cloudflare, but `wakeve.app` itself still has no public DNS answer from this network, so legal/support/terms/third-party-notices, dashboard, redirect, and AASA URLs remain unreachable. A signed archive/upload has not been produced, and App Store Connect account signing/profiles, App Store Connect role/agreement evidence, live `wakeve.app` legal/support/web/AASA DNS/deployment, App Review contact phone and uploaded-build reviewer access evidence, production Apple Team ID wiring for AASA, Apple Developer Push/Siri/Sign in with Apple/Associated Domains capabilities, App Store version/build-number comparison, signed release artifact evidence, content rights/IP evidence, App Store pricing and storefront availability evidence, third-party SDK privacy/signature evidence, App Store release control evidence, App Store media/localization evidence, license inventory and notices evidence, App Store EULA evidence, final legal privacy answers, App Store availability choices, EU DSA trader status handling, account deletion readiness, user-generated content moderation readiness, payment/external purchase compliance, accessibility label choices, TestFlight smoke testing, crash/dSYM observability evidence, live URL/AASA evidence, and the final signoff record still need external verification. The operational submission sequence is now documented in `docs/APP_STORE_SUBMISSION_RUNBOOK.md`, active blockers are tracked in `docs/APP_STORE_BLOCKER_REGISTER.md`, TestFlight/launch verification is documented in `docs/APP_STORE_LAUNCH_CHECKLIST.md`, TestFlight smoke evidence is tracked in `docs/APP_STORE_TESTFLIGHT_EVIDENCE.md`, App Store crash/backend/support observability evidence is tracked in `docs/APP_STORE_OBSERVABILITY_EVIDENCE.md`, production live legal/support/AASA/backend evidence is tracked in `docs/APP_STORE_LIVE_URL_AASA_EVIDENCE.md`, Apple Developer capability/profile evidence is tracked in `docs/APP_STORE_CAPABILITIES_EVIDENCE.md`, payment review-build evidence is tracked in `docs/APP_STORE_PAYMENT_EVIDENCE.md`, privacy label/legal evidence is tracked in `docs/APP_STORE_PRIVACY_EVIDENCE.md`, Accessibility Nutrition Label evidence is tracked in `docs/APP_STORE_ACCESSIBILITY_EVIDENCE.md`, App Store availability evidence is tracked in `docs/APP_STORE_AVAILABILITY_EVIDENCE.md`, App Store pricing and availability evidence is tracked in `docs/APP_STORE_PRICING_AVAILABILITY_EVIDENCE.md`, third-party SDK privacy/signature evidence is tracked in `docs/APP_STORE_SDK_PRIVACY_EVIDENCE.md`, App Store release control evidence is tracked in `docs/APP_STORE_RELEASE_CONTROL_EVIDENCE.md`, App Store media/localization evidence is tracked in `docs/APP_STORE_MEDIA_LOCALIZATION_EVIDENCE.md`, App Store license notices evidence is tracked in `docs/APP_STORE_LICENSE_NOTICES_EVIDENCE.md`, App Store EULA evidence is tracked in `docs/APP_STORE_EULA_EVIDENCE.md`, App Store account deletion evidence is tracked in `docs/APP_STORE_ACCOUNT_DELETION_EVIDENCE.md`, App Store UGC moderation evidence is tracked in `docs/APP_STORE_UGC_MODERATION_EVIDENCE.md`, App Store account access and agreement evidence is tracked in `docs/APP_STORE_ACCOUNT_ACCESS_EVIDENCE.md`, App Store export compliance evidence is tracked in `docs/APP_STORE_EXPORT_COMPLIANCE_EVIDENCE.md`, App Store review access evidence is tracked in `docs/APP_STORE_REVIEW_ACCESS_EVIDENCE.md`, App Store versioning/build evidence is tracked in `docs/APP_STORE_VERSIONING_EVIDENCE.md`, signed IPA/archive/dSYM/hash evidence is tracked in `docs/APP_STORE_RELEASE_ARTIFACT_EVIDENCE.md`, App Store content rights/IP evidence is tracked in `docs/APP_STORE_CONTENT_RIGHTS_EVIDENCE.md`, the final release signoff record is tracked in `docs/APP_STORE_FINAL_SIGNOFF.md`, App Store accessibility label answers are drafted in `docs/APP_STORE_ACCESSIBILITY_LABELS.md`, DSA trader status handling and evidence are tracked in `docs/APP_STORE_DSA_TRADER_STATUS.md`, Mac/Vision Pro availability decisions are recorded in `docs/APP_STORE_AVAILABILITY_DECISIONS.md`, payment compliance is tracked in `docs/APP_STORE_PAYMENT_COMPLIANCE.md`, and the App Review guideline audit is tracked in `docs/APP_STORE_REVIEW_GUIDELINE_AUDIT.md`. The final signoff variables are now validated across `docs/APP_STORE_FINAL_SIGNOFF.md`, `.env.appstore.example`, `fastlane/Fastfile`, and `scripts/app-store-submission-audit.sh`.

## Latest 2026-06-21 Refresh

- AS-07 availability: the iOS Release build settings now disable Mac Designed for iPhone/iPad and Apple Vision Pro compatibility for the first submission (`SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD = NO`, `SUPPORTS_XR_DESIGNED_FOR_IPHONE_IPAD = NO`) while keeping iPhone/iPad as the target family. App Store Connect availability still requires manual confirmation.
- AS-14 live URLs and AASA: direct production checks were refreshed on 2026-06-21 local time with capture timestamp 2026-06-20T22:48:00Z. `api.wakeve.app` resolves to Cloudflare and `https://api.wakeve.app/health` is reachable, but `wakeve.app` still has no public DNS answer from this network. Local deployable routes remain present, but this remains a submission blocker until production `wakeve.app` DNS, HTTPS, legal/support/dashboard routes, legacy redirects, AASA hosting with the real Apple Team ID, and full live URL/AASA evidence pass.
- AS-08 DSA trader status: the official Apple DSA baseline was refreshed on 2026-06-01. The local evidence now states that repository checks cannot determine Wakeve's legal trader status; App Store Connect must record verified trader details, a non-trader declaration, or intentional EU storefront exclusion before final upload.
- AS-12 TestFlight and observability: the official Apple TestFlight, tester feedback, crash-log, and dSYM baselines were refreshed on 2026-06-01. Repository evidence now records that no uploaded TestFlight build, signed IPA/archive, real-device TestFlight install, 24-hour monitoring window, or uploaded-build crash/feedback evidence exists yet, so this remains a launch blocker.
- AS-19 release control: local Fastlane and audit evidence was refreshed on 2026-06-01. The upload path still keeps App Review submission manual (`submit_for_review: false`) and public release manual (`automatic_release: false`), but App Store Connect release-option evidence, owner assignment, release window, stop/pause criteria, and first-day monitoring remain required.
- AS-21 licenses and notices: the dependency inventory and third-party notices were regenerated with 407 listed dependencies, zero unknown licenses, and no submitted iOS unknown/copyleft risks. Final human confirmation is still required before closing the blocker.
- Local unsigned Release artifact cross-check: recorded executable, shared framework, built `Info.plist`, and built `PrivacyInfo.xcprivacy` hashes still match the current unsigned Release bundle, and the app executable UUID matches the dSYM UUID.
- AS-10 UGC moderation: local implementation evidence was refreshed on 2026-06-13. Server tests now pass for hard-policy rejection, pending-review hiding, report/block/unblock flows, notification suppression, WebSocket block-filter wiring, and potential-location moderation; route guards cover event title, description, custom event type, potential locations, meals, and budget item planning text. iOS regression coverage passes for report/block/unblock discoverability and hidden/pending/rejected states. `./scripts/test-app-store-ugc-gates.sh` proves the final audit still rejects missing or forced UGC signoff without complete OpenSpec and evidence records. Uploaded-build App Review evidence remains required.

## Validated locally

- Official Apple references checked on 2026-05-27
  - Result: Apple's current SDK minimum page says that, since 2026-04-28, App Store Connect uploads must be built with Xcode 26 or later and the iOS/iPadOS 26 SDK or later: https://developer.apple.com/news/upcoming-requirements/
  - Result: Apple's App Store Connect help says Accessibility Nutrition Labels require auditing support before responding, are device-family specific, and can be left unindicated until evidence exists: https://developer.apple.com/help/app-store-connect/manage-app-accessibility/overview-of-accessibility-nutrition-labels/
  - Result: Apple's App Store Connect help says DSA trader status must be declared, and trader contact information is required for EU distribution when applicable: https://developer.apple.com/help/app-store-connect/manage-compliance-information/manage-european-union-digital-services-act-trader-requirements
- `xcodebuild -project iosApp/iosApp.xcodeproj -scheme WakeveApp -configuration Debug -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath build/xcode-deriveddata build`
  - Result: `BUILD SUCCEEDED`
- XcodeBuildMCP `build_run_sim` for `iosApp/iosApp.xcodeproj`, scheme `WakeveApp`, simulator `iPad Air 13-inch (M4)`, configuration `Debug`, and `CODE_SIGNING_ALLOWED=NO`
  - Result: `SUCCEEDED`, installed and launched bundle `com.guyghost.wakeve` on simulator process `54065`; build log: `/Users/guy/Library/Developer/XcodeBuildMCP/workspaces/wakeve-cf467b3193b0/logs/build_run_sim_2026-05-27T14-48-46-920Z_pid87182_cdf0b7fe.log`.
- XcodeBuildMCP screenshot capture after the successful iPad simulator launch
  - Result: screenshot succeeded and was copied to `docs/app-store-evidence/xcodebuildmcp-ipad-login-2026-05-27.jpg` for the login screen visual record. XcodeBuildMCP `snapshot_ui` still failed to extract the accessibility hierarchy, coordinate tapping did not advance past the login screen, and `test_sim` timed out at 120 seconds; no full iOS UI/accessibility/test readiness credit is taken from this partial evidence.
- XcodeBuildMCP `build_run_sim` for `iosApp/iosApp.xcodeproj`, scheme `WakeveApp`, simulator `iPhone 17 Pro Max`, configuration `Debug`, and `CODE_SIGNING_ALLOWED=NO`
  - Result: `SUCCEEDED`, installed and launched bundle `com.guyghost.wakeve` on simulator process `61453`; build log: `/Users/guy/Library/Developer/XcodeBuildMCP/workspaces/wakeve-cf467b3193b0/logs/build_run_sim_2026-05-27T14-55-50-958Z_pid87182_36656435.log`.
- XcodeBuildMCP iPhone onboarding screenshot and accessibility hierarchy checks
  - Result: screenshots were copied to `docs/app-store-evidence/xcodebuildmcp-iphone-onboarding-events-2026-05-27.jpg` and `docs/app-store-evidence/xcodebuildmcp-iphone-onboarding-collaboration-2026-05-27.jpg`. `snapshot_ui` succeeded on iPhone and exposed accessible labels for the `Wakeve` application, `Suivant`, `Passer`, onboarding titles, descriptive text, selected checklist items, and page slider values. This is partial iPhone evidence only; it does not replace full TestFlight, Dynamic Type, contrast, or VoiceOver validation.
- XcodeBuildMCP iPhone login screenshot and accessibility hierarchy checks
  - Result: tapping `Passer` from onboarding reached the login screen. Screenshot evidence was copied to `docs/app-store-evidence/xcodebuildmcp-iphone-login-2026-05-27.jpg`; `snapshot_ui` exposed accessible labels for `Se connecter`, `Sign in with Apple`, `Development mode: Skip authentication`, `Read Privacy Policy`, and `Read Terms of Service`. Tapping the development skip button by label and by coordinates did not leave the login screen in this simulator session.
- XcodeBuildMCP iPhone login guest-access screenshot and accessibility hierarchy check
  - Result: after adding the release-visible guest button, `build_run_sim` succeeded for Debug on `iPhone 17 Pro Max`, screenshot evidence was copied to `docs/app-store-evidence/xcodebuildmcp-iphone-login-guest-2026-05-27.jpg`, and `snapshot_ui` exposed accessible labels for `Se connecter`, `Sign in with Apple`, `Continue as guest`, `Development mode: Skip authentication`, `Read Privacy Policy`, and `Read Terms of Service`. This is DEBUG simulator evidence only; uploaded TestFlight/App Store Connect review-build evidence is still required before closing reviewer access.
- XcodeBuildMCP iPhone post-login home screenshot and accessibility hierarchy checks
  - Result: `build_run_sim` with the DEBUG-only launch argument `--wakeve-debug-authenticated` succeeded, installed and launched bundle `com.guyghost.wakeve` on simulator process `76792`; build log: `/Users/guy/Library/Developer/XcodeBuildMCP/workspaces/wakeve-cf467b3193b0/logs/build_run_sim_2026-05-27T15-10-12-796Z_pid87182_85f81657.log`. Screenshot evidence was copied to `docs/app-store-evidence/xcodebuildmcp-iphone-post-login-home-2026-05-27.jpg` at 368 x 800. `snapshot_ui` exposed accessible labels for `À venir`, `Profil`, `Aucun événement à venir`, the empty-state description, `Créer un événement`, and the tab bar. This is DEBUG simulator evidence only; it does not replace TestFlight smoke testing on a signed review build.
- XcodeBuildMCP iPhone create-event screenshot and accessibility hierarchy checks
  - Result: tapping `Créer un événement` from the authenticated empty home state opened the event creation sheet. Screenshot evidence was copied to `docs/app-store-evidence/xcodebuildmcp-iphone-create-event-2026-05-27.jpg` at 368 x 800. `snapshot_ui` exposed accessible labels for `Fermer`, disabled `Aperçu`, `Photo`, `Ajouter un arrière-plan`, `Titre de l'événement`, `Date et heure`, `Lieu`, `Organisé par Dev User`, `Ajouter une description`, `Type d'événement`, and `Créer l'événement`. This is DEBUG simulator evidence only; it does not replace Dynamic Type, contrast, VoiceOver, or TestFlight smoke testing on a signed review build.
- XcodeBuildMCP iPhone login refresh and iOS test attempt
  - Result: on 2026-05-28, XcodeBuildMCP `test_sim` with `CODE_SIGNING_ALLOWED=NO` timed out at 120 seconds. XcodeBuildMCP `build_run_sim` produced a successful Debug build log for `Wakeve.app`, but the simulator install step failed with `NSMachErrorDomain -308`; `simctl install` and `simctl launch` then started `com.guyghost.wakeve` on iPhone 17 Pro Max. Screenshot evidence was copied to `docs/app-store-evidence/xcodebuildmcp-iphone-login-refresh-2026-05-28.jpg` at 368 x 800, and `snapshot_ui` exposed accessible labels for `Se connecter`, `Sign in with Apple`, `Continue as guest`, `Development mode: Skip authentication`, `Read Privacy Policy`, and `Read Terms of Service`. This remains DEBUG simulator evidence only.
- XcodeBuildMCP iPhone test and high-contrast Dynamic Type refresh
  - Result: on 2026-05-28, XcodeBuildMCP `test_sim` with `CODE_SIGNING_ALLOWED=NO` completed 94 tests, 94 passed, 0 failed. Build log: `/Users/guy/Library/Developer/XcodeBuildMCP/workspaces/wakeve-cf467b3193b0/logs/test_sim_2026-05-28T02-37-15-278Z_pid87182_d99033a3.log`; result bundle: `/Users/guy/Library/Developer/XcodeBuildMCP/workspaces/wakeve-cf467b3193b0/result-bundles/test_sim_2026-05-28T02-37-15-278Z_pid87182_869fa565.xcresult`.
  - Result: XcodeBuildMCP `build_run_sim` with `CODE_SIGNING_ALLOWED=NO` succeeded on iPhone 17 Pro Max. Authenticated-home screenshot evidence was copied to `docs/app-store-evidence/xcodebuildmcp-iphone-post-login-home-2026-05-28.jpg`. A high-contrast, `accessibility-extra-extra-extra-large` Dynamic Type pass exposed a visual truncation in the home empty-state subtitle; `HomeEmptyStateView` was updated to hide decorative imagery at accessibility sizes and keep text vertically expandable. Fixed evidence was copied to `docs/app-store-evidence/xcodebuildmcp-iphone-home-high-contrast-axxxl-fixed-no-truncation-2026-05-28.jpg`, and `snapshot_ui` exposed the full empty-state description.
- `xcodebuild -project iosApp/iosApp.xcodeproj -scheme WakeveApp -configuration Release -destination 'generic/platform=iOS' -derivedDataPath build/xcode-deriveddata-release CODE_SIGNING_ALLOWED=NO build`
  - Result: `BUILD SUCCEEDED`
- `bundle exec fastlane ios preflight`
  - Result: passed. This validates iOS metadata, rebuilds Release for generic iOS without signing, fails if the latest Release xcodebuild activity log contains `warning:` or `error:` diagnostics, runs the iOS store linter, and runs the web audit/check/build through pinned `npx --yes pnpm@10` commands.
- Latest Release activity-log warning scan
  - Result: no Swift, AppIntents metadata extraction, or Xcode `warning:` diagnostics remain in the latest Release build log.
- `./gradlew :shared:compileKotlinIosArm64 --warning-mode all`
  - Result: `BUILD SUCCESSFUL`. Kotlin source warnings for iOS shared code are cleared; remaining uppercase Gradle warnings are build-tool configuration deprecations.
- `.github/workflows/store-readiness.yml`
  - Result: now includes a macOS App Store preflight job that installs Bundler, pnpm, and the pinned OpenSpec CLI, audits web dependencies at low severity, syntax-checks the store/audit scripts, validates the active App Store OpenSpec proposals for account deletion and user-generated content moderation, checks that the blocker register links those proposals, runs `bundle exec fastlane ios preflight`, verifies strict preflight behavior with or without App Review phone configuration, verifies that the documented App Review phone placeholder is rejected by `submission_ready`, `upload_appstore`, and the final non-uploading audit, verifies that the documented Apple release placeholders are rejected by `submission_ready`, `upload_testflight`, and the final audit, verifies that TestFlight rejects those placeholders before running preflight, verifies that `upload_appstore` rejects an incomplete final signoff record even if the marker is forced, verifies that this early rejection happens before the final audit is launched, and has linter coverage requiring those rejection steps. The CI pnpm 10 path was checked locally with `CI=true npx --yes pnpm@10 install --frozen-lockfile`, `CI=true npx --yes pnpm@10 audit --audit-level low`, `CI=true npx --yes pnpm@10 check`, and `CI=true npx --yes pnpm@10 build`.
- `./scripts/app-store-submission-audit.sh --skip-preflight`
  - Expected current result: exits non-zero with 22 blockers and 1 warning until Apple environment variables, App Review phone, manual signoffs, live URLs, signed submission readiness, account deletion readiness, user-generated content moderation readiness, payment/external purchase compliance, third-party SDK privacy/signature evidence, App Store release control evidence, App Store media/localization evidence, license notices evidence, EULA evidence, and the final signoff record are complete. With a valid non-placeholder `APP_REVIEW_PHONE_NUMBER`, the current blocker count drops to 21. The script is the aggregated non-uploading final audit, validates Apple release variable formats when values are supplied, rejects documented template placeholders, and cannot report ready if local preflight, live URL validation, or `submission_ready` is skipped.
- `APP_REVIEW_PHONE_NUMBER='+33123456789' ./scripts/app-store-submission-audit.sh`
  - Result: local App Store preflight ran and passed, then the audit exited non-zero with 21 blockers and 0 warnings. Result: NOT READY for App Store submission. Remaining blockers are the expected Apple environment variables, manual signoffs, live URL/AASA validation, signed final gate, pricing/storefront availability evidence, third-party SDK privacy/signature evidence, App Store release control evidence, App Store media/localization evidence, license notices evidence, EULA evidence, account deletion review-build evidence, UGC moderation review-build evidence, payment/external purchase compliance, TestFlight evidence, and Apple Developer capability/profile evidence.
- `xcodebuild -version` and `xcodebuild -version -sdk iphoneos SDKVersion`
  - Result: Xcode 26.5 and iOS SDK 26.5.
- `plutil -lint iosApp/src/Info.plist iosApp/src/PrivacyInfo.xcprivacy iosApp/src/Wakeve.entitlements`
  - Result: all files are valid XML plists.
- `plutil -p iosApp/src/Info.plist | rg -n "NSSiriUsageDescription|INIntentsSupported"` and `plutil -p iosApp/src/Wakeve.entitlements`
  - Result: Siri usage text is present, Siri intents are declared, APNs uses `$(APS_ENVIRONMENT)`, `com.apple.developer.siri=true` is declared, Sign in with Apple declares `Default`, and Associated Domains declares `applinks:wakeve.app`.
- `xcodebuild -project iosApp/iosApp.xcodeproj -scheme WakeveApp -configuration Release -destination 'generic/platform=iOS' -showBuildSettings | rg -n "CODE_SIGN_ENTITLEMENTS|APS_ENVIRONMENT|DEVELOPMENT_TEAM|PRODUCT_BUNDLE_IDENTIFIER|PROVISIONING_PROFILE|CODE_SIGN_STYLE|CODE_SIGN_IDENTITY"` and `codesign -d --entitlements :- build/xcode-deriveddata-release/Build/Products/Release-iphoneos/Wakeve.app`
  - Result: Release build settings show `APS_ENVIRONMENT = production`, `CODE_SIGN_ENTITLEMENTS = src/Wakeve.entitlements`, `CODE_SIGN_STYLE = Automatic`, `CODE_SIGN_IDENTITY = Apple Development`, `PRODUCT_BUNDLE_IDENTIFIER = com.guyghost.wakeve`, and `PROVISIONING_PROFILE_REQUIRED = YES`; the checked local Release bundle reports `code object is not signed at all`, so this is source/build-setting evidence only and not signed IPA or Apple Developer profile proof.
- `./scripts/lint-store-metadata.sh --ios-only`
  - Result: 3106 checks passed, 0 errors, 1 warning when a non-placeholder `APP_REVIEW_PHONE_NUMBER` is supplied. Source code using Siri, speech recognition, When In Use location, calendar access, photo selection, photo library write access, camera capture, remote notifications, local debug API URLs, and third-party login is matched against required `Info.plist` purpose strings, production network fallbacks, and Sign in with Apple availability/entitlement. No-tracking privacy claims are checked against `NSPrivacyTracking=false`, empty tracking domains, absence of `NSUserTrackingUsageDescription`, absence of IDFA/App Tracking Transparency source usage, absence of IDFA/App Tracking Transparency strings in the Release app binaries, absence of development-auth credentials in the Release app binaries, DEBUG-only isolation for development auth hooks, absence of `TODO`/`FIXME` markers in active iOS launch source, absence of raw Swift `print`/`debugPrint`/`NSLog` calls outside the DEBUG-only logger, DEBUG-only isolation for unconfigured BackgroundTasks source, absence of unconfigured BackgroundTasks symbols in the Release binary, absence of unsupported public in-app account deletion promises before a verified iOS/API deletion flow exists, absence of unnecessary platform trademark/design-system references in iOS App Store descriptions, documented payment/external purchase compliance for payment/Tricount surfaces, App Review notes explaining the payment model, absence of StoreKit/IAP source patterns for the free/no-IAP first-release recommendation, recorded local unsigned Release artifact hashes matching the current local build, and strict OpenSpec validation for the active account-deletion and UGC-moderation proposals. Fastlane App Store upload guards are checked so final signoffs, final signoff record enforcement, mandatory final audit execution from `upload_appstore`, placeholder App Review phone rejection in final gates and the final audit, documented Apple release placeholder rejection in App Store and TestFlight paths, incomplete final signoff record rejection, forced DSA/pricing/SDK/release-control/media/licenses/eula/product/account/review/export/app-information/versioning/release-artifact/content-rights/observability/live-url-aasa final evidence rejection in CI, Release xcodebuild diagnostic scanning, strict/live preflight, TestFlight preflight-before-build execution, pinned pnpm 10 web dependency audit/check/build, local public legal/AASA route verification, signed IPA entitlement inspection, screenshots, age rating config, App Review contact information upload, manual App Review submission, manual release, DSA trader status confirmation with evidence, pricing and availability evidence, SDK privacy/signature evidence, App Store release control evidence, App Store media/localization evidence, license inventory and notices evidence, App Store EULA evidence, account deletion review-build evidence, UGC moderation review-build evidence, and payment compliance stay enforced. Xcode Release build settings are checked for Bundle ID, product name, source plist, entitlements, APNs production environment, marketing/build versions, iPhoneOS 27 SDK, deployment target, iPhone/iPad targeting, app icon, documented Mac/Vision Pro availability, App Review guest/demo access notes, App Review phone env/file configuration, Privacy Manifest required-reason API coverage, App Store privacy label draft alignment with the privacy manifest, privacy label/legal evidence coverage, public legal/support web route coverage including `/third-party-notices`, App Store account access evidence coverage, App Store account access local gate coverage, App Store app information evidence coverage, App Store versioning evidence coverage, App Store release artifact evidence coverage, App Store content rights/IP evidence coverage, App Store pricing and availability evidence coverage, App Store SDK privacy evidence coverage, App Store release control evidence coverage, App Store media/localization evidence coverage, license inventory draft coverage, third-party notices draft coverage, public notices route coverage, App Store license inventory and notices evidence coverage, App Store EULA evidence coverage, App Store account deletion evidence coverage, App Store UGC moderation evidence coverage, App Store observability evidence coverage, local unsigned dSYM evidence coverage, App Store live URL/AASA evidence coverage, App Store availability decision evidence, App Store availability evidence coverage, DSA trader status evidence, App Store Accessibility Nutrition Label draft coverage, Accessibility Nutrition Label evidence coverage with local simulator artifact references, TestFlight launch checklist coverage, TestFlight crash/dSYM monitoring coverage, Apple Developer capability/profile evidence coverage, local unsigned capability-scan evidence coverage, payment review-build evidence coverage, App Store export compliance evidence coverage, App Store review access evidence coverage, pinned Fastlane version, Bundle ID, package name, Apple environment variables, final signoff record marker, blocker evidence matrix, App Store Connect field map coverage, App Store age rating evidence coverage including the 2026 updated questionnaire, blocker register coverage including the refreshed 2026-06-20 live URL/AASA state, live third-party notices endpoint coverage, AASA placeholder Team ID rejection, App Store preflight workflow coverage including frozen pnpm install and low-severity web audit plus placeholder rejection, App Review guideline audit, App Store payment compliance record, App Store submission runbook coverage including the final audit release/signoff environment, safe App Store environment template defaults and evidence mapping, `.env.appstore` ignore rules, App Review phone coverage in final copied commands, Store Readiness Summary blocker messaging, the final signoff variable contract across the docs, environment template, Fastlane upload path, and final audit, the top-level App Store readiness report's Apple-source baseline, refreshed TestFlight metric/feedback/crash-report evidence requirements, refreshed Apple Developer capability/profile evidence requirements, refreshed App Store release-control/rollback evidence requirements, refreshed live URL/AASA deployment-blocker evidence requirements, refreshed account-deletion evidence requirements, refreshed UGC-moderation evidence requirements, refreshed media/localization screenshot and app-preview evidence requirements, refreshed media/localization scaling, Media Manager, and editable-status evidence requirements, refreshed EULA/Terms evidence requirements, refreshed export-compliance source-scan evidence requirements, refreshed App Information record/localization evidence requirements, refreshed version/build-selection evidence requirements, refreshed signed release artifact evidence requirements, refreshed content-rights/IP evidence requirements, refreshed observability crash/dSYM evidence requirements, refreshed App Review access evidence requirements, refreshed final-signoff App Store status evidence requirements, refreshed blocker-register App Store submission-state evidence requirements, refreshed DSA trader-status evidence requirements, refreshed pricing/availability distribution evidence requirements, tightened release-control/pre-order/phased-release evidence requirements, and refreshed SDK privacy/signature/repackaged-SDK evidence requirements.
- `plutil -p build/xcode-deriveddata-release/Build/Products/Release-iphoneos/Wakeve.app/Info.plist | rg -n "ITSAppUsesNonExemptEncryption|NSLocation|UIBackgroundModes|UIRequiredDeviceCapabilities"`
  - Result: built Release app contains `ITSAppUsesNonExemptEncryption=false`, `NSLocationWhenInUseUsageDescription`, `UIBackgroundModes=[remote-notification]`, and `UIRequiredDeviceCapabilities=[arm64]`.
- `plutil -p build/xcode-deriveddata-release/Build/Products/Release-iphoneos/Wakeve.app/Info.plist | rg -n "CFBundleShortVersionString|CFBundleVersion|CFBundleURLTypes|wakeve|CFBundleIdentifier|CFBundleDisplayName"`
  - Result: built Release app contains Bundle ID `com.guyghost.wakeve`, display name `Wakeve`, marketing version `1.0`, build version `1`, and the `wakeve` URL scheme.
- `test -f build/xcode-deriveddata-release/Build/Products/Release-iphoneos/Wakeve.app/PrivacyInfo.xcprivacy`
  - Result: `PrivacyInfo.xcprivacy` is bundled in the built Release app.
- `plutil -p build/xcode-deriveddata-release/Build/Products/Release-iphoneos/Wakeve.app/PrivacyInfo.xcprivacy | rg -n "NSPrivacyCollectedDataType(Name|EmailAddress|UserID|DeviceID|OtherUserContent|CoarseLocation|ProductInteraction)|NSPrivacyTracking"`
  - Result: built Release privacy manifest declares name, email, user ID, device ID, other user content, coarse location, product interaction, and `NSPrivacyTracking=false`.
- `/usr/bin/strings build/xcode-deriveddata-release/Build/Products/Release-iphoneos/Wakeve.app/Wakeve build/xcode-deriveddata-release/Build/Products/Release-iphoneos/Wakeve.app/Frameworks/Shared.framework/Shared | rg -n "localhost|127\.0\.0\.1|http://"`
  - Result: no matches in the built Release app binaries.
- `ruby -rjson -e 'JSON.parse(File.read("iosApp/src/Assets.xcassets/AppIcon.appiconset/Contents.json"))'`
  - Result: app icon asset catalog JSON is valid.
- `magick identify -format '%f %wx%h %[channels]\n' iosApp/src/Assets.xcassets/AppIcon.appiconset/*.png`
  - Result: all app icon PNGs are 1024 x 1024 RGB with no alpha channel.
- `./scripts/lint-store-metadata.sh --strict --ios-only`
  - Result: 3106 checks passed, 0 errors, 1 warning when `APP_REVIEW_PHONE_NUMBER` is supplied. App Store metadata, privacy/support URLs, legal docs, Fastlane config, copyright, app rating JSON/schema, App Review notes/contact name/email, screenshots, plist submission settings, custom URL scheme registration, Siri/speech/location/photo/camera usage declarations, Siri/Sign in with Apple/Associated Domains capability declarations, repo-side AASA routing, privacy manifest declarations, no-tracking/IDFA checks, iOS SDK upload minimum, bundled privacy manifest, built Bundle ID/display name/version/build number, Release binary network-string scan, the top-level App Store readiness report's Apple-source baseline, refreshed TestFlight metric/feedback/crash-report evidence requirements, refreshed Apple Developer capability/profile evidence requirements, refreshed App Store release-control/rollback evidence requirements, refreshed live URL/AASA deployment-blocker evidence requirements, refreshed account-deletion evidence requirements, refreshed UGC-moderation evidence requirements, refreshed media/localization screenshot and app-preview evidence requirements, refreshed media/localization scaling, Media Manager, and editable-status evidence requirements, refreshed EULA/Terms evidence requirements, refreshed export-compliance source-scan evidence requirements, refreshed App Information record/localization evidence requirements, refreshed version/build-selection evidence requirements, refreshed signed release artifact evidence requirements, refreshed content-rights/IP evidence requirements, refreshed observability crash/dSYM evidence requirements, refreshed App Review access evidence requirements, refreshed final-signoff App Store status evidence requirements, refreshed blocker-register App Store submission-state evidence requirements, refreshed DSA trader-status evidence requirements, refreshed pricing/availability distribution evidence requirements, tightened release-control/pre-order/phased-release evidence requirements, and refreshed SDK privacy/signature/repackaged-SDK evidence requirements are valid. It exits non-zero in strict mode unless App Review contact phone is configured through `APP_REVIEW_PHONE_NUMBER` or the fallback metadata file; the current supplied-phone run still exits non-zero because the intentionally incomplete final signoff record remains a strict-mode warning.
- `APP_REVIEW_PHONE_NUMBER='+33123456789' APPLE_TEAM_ID='A1B2C3D4E5' ./scripts/lint-store-metadata.sh --ios-only --check-live-urls`
  - Result: 3107 checks passed, 17 errors, 1 warning, and exits non-zero because `wakeve.app` is still not publicly reachable for `https://wakeve.app/privacy`, `https://wakeve.app/support`, `https://wakeve.app/terms`, `https://wakeve.app/third-party-notices`, dashboard `/app` routes, legacy redirects, and both AASA URLs. `https://api.wakeve.app/health` is now publicly reachable, so the remaining live URL/AASA blocker is the `wakeve.app` web/AASA scope plus production Apple Team ID evidence. Privacy and support URLs are checked once per iOS locale, so the live URL failure currently contributes four locale URL errors plus terms, third-party notices, five dashboard route errors, four legacy redirect errors, and both AASA path errors. When reachable, each AASA check also validates JSON, `application/json`, Bundle ID, Apple Team ID app ID when `APPLE_TEAM_ID` is set, and the expected Universal Link path groups. Other local iOS checks pass with the intentionally incomplete final signoff record warning.
- Local web route validation with `APPLE_TEAM_ID=A1B2C3D4E5 ./node_modules/.bin/vite dev --host 127.0.0.1 --port 4174` and `BASE_URL=http://127.0.0.1:4174 APPLE_TEAM_ID=A1B2C3D4E5 ./scripts/app-store-local-web-route-check.sh`
  - Result: `/privacy`, `/support`, `/terms`, and `/third-party-notices` returned status 200 with the expected App Store review phrases in the initial HTML after enabling `ssr = true` on those public pages. `/.well-known/apple-app-site-association` and `/apple-app-site-association` returned status 200, `application/json`, app ID `A1B2C3D4E5.com.guyghost.wakeve`, and `/event/*`, `/poll/*`, `/meeting/*`, plus `/invite/*`. This is local pre-deployment evidence only and does not close the live URL/AASA blocker.
- `./scripts/lint-store-metadata.sh --strict`
  - Result: 2986 checks passed, 0 errors, 3 warnings when `APP_REVIEW_PHONE_NUMBER` is supplied. The final App Store readiness decision is still enforced by `scripts/app-store-submission-audit.sh`, which remains non-ready until live URL/AASA validation and the signed final gate run with external Apple evidence.
- `bundle exec fastlane ios validate_metadata`
  - Result: `fr-FR` and `en-US` iOS metadata valid; `fr-FR` and `en-US` screenshots valid.
- `bundle exec fastlane ios build` without `TEAM_ID`
  - Result: exits immediately with `iOS signing requires environment variable(s): TEAM_ID`. When signing is configured, the build lane now inspects the generated IPA entitlements before any upload path can continue.
- `bundle exec fastlane ios upload_appstore`
  - Expected behavior: the lane now requires `APPLE_ID`, `ITC_TEAM_ID`, `TEAM_ID`, `APPLE_TEAM_ID`, and the final manual signoff variables, runs `preflight(strict: true, live_urls: true)`, then builds and uploads only after strict local, web, live URL, signing, privacy, accessibility, availability, DSA trader status, account deletion readiness, user-generated content moderation, payment/external purchase compliance, TestFlight, and capability gates pass.
- `cd apps/landing && npx --yes pnpm@10 build`
  - Result: web app production build passes with `@sveltejs/adapter-vercel` and includes `/privacy`, `/terms`, `/support`, `/.well-known/apple-app-site-association`, and `/apple-app-site-association`.
- `cd apps/landing && npx --yes pnpm@10 check`
  - Result: SvelteKit type and component checks pass with 0 errors and 0 warnings.
- `cd apps/landing && pnpm audit --audit-level moderate`
  - Result: exits zero with no high or moderate advisories.
- `cd apps/landing && pnpm audit --audit-level low`
  - Result: exits zero with no known vulnerabilities after the `cookie@<0.7.0 -> 0.7.2` pnpm override.
- `cd apps/landing && pnpm peers check`
  - Result: no peer dependency issues found.
- `cd apps/landing && CI=true npx --yes pnpm@10 install --frozen-lockfile`, `audit --audit-level low`, `check`, `build`, and `why cookie`
  - Result: the CI pnpm version accepts the current lockfile, audits with no known vulnerabilities, type-checks and builds successfully, and resolves SvelteKit's transitive `cookie` package to `0.7.2`.
- `APPLE_TEAM_ID=A1B2C3D4E5 pnpm preview --host 127.0.0.1 --port 4173`, then `curl -i http://127.0.0.1:4173/.well-known/apple-app-site-association` and `/apple-app-site-association`
  - Result: both endpoints respond `200 OK`, `content-type: application/json`, and include app ID `A1B2C3D4E5.com.guyghost.wakeve` with `/event/*`, `/poll/*`, `/meeting/*`, and `/invite/*` components.
- Browser check: `http://localhost:3000/support`
  - Result: page responds locally with title `Support - Wakeve`.
- `xcrun simctl io 1AD5D004-5B52-4EDC-A893-7F6D0B02A4CF screenshot composeApp/metadata/ios/en-US/screenshots/01-iphone-home.png`
  - Result: iPhone Pro Max screenshot captured at 1320 x 2868 and copied to `fr-FR`.
- `xcrun simctl io C9646394-E6CB-4C05-8BCB-54FE1D1B4406 screenshot composeApp/metadata/ios/en-US/screenshots/02-ipad-home.png`
  - Result: iPad Air 13-inch screenshot captured at 2048 x 2732 and copied to `fr-FR`.
- `find composeApp/metadata/ios composeApp/screenshots/ios -type f -name '*.png'` with `sips -g pixelWidth -g pixelHeight`
  - Result: all iOS screenshots are App Store accepted sizes: iPhone screenshots are 1320 x 2868, and iPad screenshots are 2048 x 2732.
- `dig +short wakeve.app`, `dig +short api.wakeve.app`, `curl -I --max-time 12 https://wakeve.app/privacy`, `curl -I --max-time 12 https://api.wakeve.app/health`, and `curl --max-time 12 -i https://api.wakeve.app/health`
  - Result: `wakeve.app` still has no DNS answer and `https://wakeve.app/privacy` fails with `Could not resolve host: wakeve.app`; `api.wakeve.app` resolves to Cloudflare (`104.21.48.204`, `172.67.156.46`), `HEAD /health` returns HTTP `405`, and `GET /health` returns HTTP `200 OK`. Public legal/support/AASA pages remain not live from this environment, while backend health is reachable and must stay review-accessible.

## Fixed during this pass

- Added iOS permission purpose strings for camera and photo library access in `iosApp/src/Info.plist`.
- Added `iosApp/src/PrivacyInfo.xcprivacy` with required-reason API declarations for file timestamps and UserDefaults usage.
- Extended the store linter so detected iOS/KMP `UserDefaults`, `NSUserDefaults`, `@AppStorage`, or `@SceneStorage` usage must be backed by `NSPrivacyAccessedAPICategoryUserDefaults` with reason `CA92.1`, and detected file timestamp APIs must be backed by `NSPrivacyAccessedAPICategoryFileTimestamp` with reason `C617.1`.
- Updated `iosApp/src/PrivacyInfo.xcprivacy` so collected data declarations are no longer empty. It now declares name, email address, user ID, device ID, other user content, coarse location, and product interaction, all with `Tracking=false`.
- Added linter coverage that requires `docs/APP_STORE_PRIVACY_LABELS.md` to declare no tracking and mirror every collected data type declared in `iosApp/src/PrivacyInfo.xcprivacy`.
- Added an Apple-source baseline to the App Store privacy labels draft, dated 2026-05-27, so privacy-label closure now explicitly covers App Store privacy-detail submission requirements, third-party partner practices, accurate/up-to-date responses, collection and optional-disclosure rules, app-functionality declarations, linked-to-user identity decisions, tracking/data-broker definitions, on-device-only non-tracking treatment, required privacy policy URL coverage, app-level comprehensive answers, and new-data-type publishing setup.
- Added `docs/APP_STORE_PRIVACY_EVIDENCE.md` and final audit coverage so `APP_STORE_PRIVACY_SIGNOFF=true` also requires `APP_STORE_PRIVACY_EVIDENCE_COMPLETE=true` for App Store Connect labels, the live privacy policy, the privacy manifest, Release binary no-tracking checks, and legal/privacy approval.
- Added an Apple-source baseline to AS-05 privacy evidence, dated 2026-05-27, so privacy closure now explicitly covers required App Store privacy details for new apps/updates, third-party partner practices, ongoing answer accuracy, Apple's definition of collection, on-device-only processing, derived data sent off device, linked-data answers, tracking answers, and the required public Privacy Policy URL.
- Added an Apple-source baseline to AS-06 accessibility evidence, dated 2026-05-27, so Accessibility Nutrition Label closure now explicitly covers voluntary/current versus future-required reporting, product-page/device-specific display, unsupported-device "not yet indicated" behavior, per-device app audits, common-task completion before claiming a feature, App Review correction risk for misleading labels, ongoing answer accuracy, accessibility URL usage, and how compatible iPhone/iPad labels appear on Apple Vision Pro and Mac App Store.
- Added an Apple-source baseline to AS-07 availability evidence, dated 2026-05-27, so Mac Apple silicon and Apple Vision Pro availability closure now explicitly covers the default availability behavior, same-framework/runtime compatibility claims, app-level availability scope, Pricing and Availability locations, required App Store Connect roles, Mac compatibility verification, the uploaded-build prerequisite, and the visionOS Kids-category restriction.
- Added an Apple-source baseline to AS-08 DSA trader-status evidence, dated 2026-05-27, so EU DSA closure now explicitly covers trader contact display, the 27 EU territories, the requirement to declare trader status even outside EU distribution, new-app submission disclosure prompts, developer self-assessment, non-trader consumer-rights disclosure, payment account details, EU-law certification, and the Account Holder/Admin role requirement.
- Refreshed AS-08 DSA trader-status evidence to the 2026-05-28 Apple-source baseline, so closure now also requires app-specific DSA status for `com.guyghost.wakeve`, trader self-assessment factors, organization-versus-individual contact requirements, payment-account evidence when required, EU-law certification, optional Labels and Markings URL handling, and explicit cross-checks against pricing and availability evidence.
- Added an Apple-source baseline to AS-17 pricing and availability evidence, dated 2026-05-27, so pricing closure now explicitly covers the pre-review pricing requirement, Paid Apps Agreement gating, free-only behavior when the latest agreement is not accepted, required App Store Connect roles, price points, base country/region pricing, automatic versus manual storefront pricing, scheduled price changes, pre-order eligibility, storefront availability, education/business distribution, custom app timing, tax category defaults, and last compatible version settings.
- Refreshed AS-17 pricing and availability evidence to the 2026-05-28 Apple-source baseline, so closure now also requires App Store availability before review, 175-country/region availability selection, All Countries or Regions versus Specific Countries or Regions, future-country availability intent, pre-order location constraints, removal-from-sale effects, 24-hour visibility timing, public/private/unlisted distribution method evidence, volume purchase and education discount choices, and any new-app-record or unlisted-link implications.
- Added an Apple-source baseline to AS-11 payment evidence, dated 2026-05-27, so payment closure now explicitly covers prohibited custom digital unlock mechanisms, StoreKit external purchase link entitlements, United States storefront exception notes, multiplatform-service IAP parity, person-to-person versus one-to-many services, physical goods/services consumed outside the app, companion-app purchase limits, advertising-management exceptions, and App Review notes for unreviewable IAP items.
- Refreshed AS-11 payment evidence to the 2026-05-28 Apple-source baseline and tightened local payment behavior, so closure now explicitly covers non-US storefront external-purchase-link limits, absence of StoreKit/IAP/paywall APIs across iOS/shared/server/web source, Tricount-only external payment handoff for the first submission, rejection of non-Tricount payment-pot provider URLs, and the passing `EventOrganizationPhase5ReadinessTest` regression coverage for that rejection.
- Added an Apple-source baseline to AS-13 capabilities evidence, dated 2026-05-27, so capability/profile closure now explicitly covers explicit App IDs, App ID capability allow-lists, matching Xcode target capabilities, Account Holder/Admin role requirements, provisioning profile regeneration after App ID changes, platform-wide profile impact, additional setup for Sign in with Apple and push notifications, App Store Connect explicit-App-ID upload requirements, single distribution certificate profiles, automatic signing, profile service authorization, and managed capability approval/profile inclusion.
- Refreshed AS-13 capabilities evidence to the 2026-05-28 Apple-source baseline, so capability/profile closure now also explicitly covers Xcode entitlement and Info.plist edits, manual Identifier configuration, explicit App ID matching for App Store Connect provisioning profiles, Sign in with Apple primary/grouped App ID setup, distribution certificate evidence, and the exact single-distribution-certificate requirement.
- Refreshed AS-19 release-control evidence to the 2026-05-28 Apple-source baseline, so release closure now explicitly covers release-option role requirements, per-platform manual releases, Pending Developer Release 30-day reminders, Cancel This Release before propagation, phased-release percentages, manual download availability during phased release, legal/usability issue response, fix-update/removal paths, and the fact that App Store rollback to a previous version is unavailable.
- Tightened AS-19 release-control evidence against the 2026-05-28 Apple-source baseline, so closure now also requires manual release when pre-order is configured, pre-order country/region release behavior, phased-release automatic-update eligibility notes, no participant notification, remove-from-sale consequences, and explicit proof that removing the app from sale makes phased release unavailable again for that version.
- Added an Apple-source baseline to AS-15 export compliance evidence, dated 2026-05-27, so export compliance closure now explicitly covers App Store Connect encryption-requirement determination, Apple OS versus proprietary/non-Apple crypto, no-documentation cases, French declaration and CCATS cases, France-only declaration scope, `ITSAppUsesNonExemptEncryption` questionnaire behavior, `false` meaning no or exempt encryption, Apple-provided compliance codes after successful review, and App Store Connect API app-encryption declaration implications.
- Refreshed AS-15 export compliance evidence to the 2026-05-28 Apple-source baseline, so closure now explicitly covers developer responsibility for exemption accuracy, per-version App Store Connect encryption questions, HTTPS/`NSURLSession` as OS encryption evidence, the app-plus-linked-libraries scope of `ITSAppUsesNonExemptEncryption=false`, Keychain/Security-framework token storage, absence of iOS custom crypto API usage, and the requirement to recheck linked third-party libraries in the submitted build.
- Added an Apple-source baseline to AS-12 TestFlight evidence, dated 2026-05-27, so TestFlight closure now explicitly covers beta distribution and feedback, resolving issues before App Review, the 90-day build window, provisioning-profile application identifiers, internal/external tester limits, external TestFlight App Review, required beta app description and feedback email, required App Store Connect roles, TestFlight Internal Only limits, feedback visibility in App Store Connect, and Xcode crash-feedback access.
- Refreshed AS-12 TestFlight evidence to the 2026-05-28 Apple-source baseline, so TestFlight closure now also explicitly covers tester metric availability after install, up to 24-hour metric latency, screenshot/crash/general feedback capture, 120-day crash-report availability, and the unresponsive-crash missing-report caveat.
- Added an Apple-source baseline to AS-14 live URL/AASA evidence, dated 2026-05-27, so Universal Links closure now explicitly covers the two-way website/app association, matching AASA app IDs and Associated Domains entitlements, one AASA file per subdomain, the extensionless `.well-known` AASA path, HTTPS valid-certificate/no-redirect hosting, `applinks` service app identifiers, entitlement entry format restrictions, Apple CDN fetch/update behavior, `NSUserActivityTypeBrowsingWeb` handling, and URL-parameter validation against malformed-link attacks.
- Refreshed AS-14 live URL/AASA evidence to the 2026-05-28 Apple-source baseline, so production URL closure now explicitly records the current 9 live URL errors, Apple reference links, the distinction between deployable local routes and unreachable production domains, and the DNS/TLS/hosting/backend/AASA deployment fix checklist required before retrying the live App Store gate.
- Refreshed AS-09 account-deletion evidence to the 2026-05-28 Apple-source baseline, so closure now explicitly covers easy-to-find in-app deletion, full-account deletion instead of deactivation, guest/automatic accounts, manual or delayed deletion transparency, all-user availability regardless of location, shared UGC handling, and the fact that OpenSpec validation is proposal-readiness only until the implementation tasks are complete.
- Refreshed AS-10 UGC moderation evidence to the 2026-05-28 Apple-source baseline, so closure now explicitly covers required filtering/report/block/contact protections, live final moderation/support URLs, disabled-surface documentation for any review-build UGC shutdown, and the distinction between a valid OpenSpec proposal and implemented reviewer-visible moderation controls.
- Added an Apple-source baseline to the App Store readiness report, dated 2026-05-28, so the top-level readiness status now explicitly covers current SDK requirements, processed uploaded builds, required metadata/build selection, submitter role, Add for Review versus Submit for Review, final metadata and functional URL requirements, placeholder-content removal, on-device testing, demo access, live backend services, UGC protections, in-app account deletion, App Privacy accuracy, IAP versus outside-app payment rules, availability setup, EU DSA trader status, and App Store Connect status/build evidence.
- Added an Apple-source baseline to AS-21 license notices evidence, dated 2026-05-27, so license/notices closure now explicitly covers final App Review submissions, fully functional URLs, no placeholder or temporary content, metadata accuracy across privacy information, description, screenshots, and previews, ownership or licensing for all app content, protected third-party material restrictions, no misleading/copycat names or metadata, submitting-entity rights responsibility, service responsibility, and App Review information coverage for rights/access evidence.
- Added an Apple-source baseline to AS-01 account access evidence, dated 2026-05-27, so account/access closure now explicitly covers App Store Connect submission/app-management/legal-agreement scope, role-based privileges, Account Holder agreement responsibilities, required two-factor or two-step authentication, required roles for manual submission and build selection, build-upload role requirements, bundle/version/build-string association, user and role management permissions, free-app distribution under the Apple Developer Program License Agreement, and Paid Apps Agreement requirements for paid apps or In-App Purchases.
- Added an Apple-source baseline to AS-16 review-access evidence, dated 2026-05-27, so App Review access closure now explicitly covers required review details, valid demo credentials when sign-in-gated features exist, active demo account or fully featured demo mode requirements, required review resources, contact information, non-expiring demo accounts, review notes, per-version content review, explicit App Review submission, and accuracy of review information against app behavior.
- Added an Apple-source baseline to AS-15 App Information evidence, dated 2026-05-27, so App Information closure now explicitly covers new app record fields, Bundle ID uniqueness, generated Apple ID, non-public SKU, primary-language fallback behavior, App Store Connect/Xcode category alignment, required age rating, age-rating questionnaire flow, Apple global and region-specific age ratings, immutable Made for Kids selection after approval, and the fact that Unrated apps cannot be published on the App Store.
- Added an Apple-source baseline to AS-15 versioning evidence, dated 2026-05-27, so version/build closure now explicitly covers supported build-upload paths, Apple processing before App Store Connect visibility, Bundle ID/version association with the app version record, unique build strings, incremental App Store version numbers, Xcode build-string increments before upload, adding the build to the latest version before App Review, required metadata plus build choice, Build-section verification, one selected build per app version, changing the selected build before submission, and App Store Connect build metadata including version, build number, upload status, and creation date.
- Refreshed AS-15 versioning evidence to the 2026-05-28 Apple-source baseline, so version/build closure now explicitly covers required upload and build-selection roles, build-processing completion email, failed-upload build-number reuse limits, selected Build-section icon/version/build/upload-date evidence, Missing Compliance resolution before review submission, and App Store Connect API build lookup references.
- Added an Apple-source baseline to AS-15 release-artifact evidence, dated 2026-05-27, so signed artifact closure now explicitly covers app-record prerequisites, supported upload tools, delivery progress/logs, Apple build processing and completion email, Bundle ID/version association, TestFlight/App Review availability, build metadata and file sizes, upload status fields, Xcode archive binaries and dSYMs, optional symbol upload for TestFlight/App Store distribution, symbolicated crash-log needs, App Store Connect dSYM downloads when available, Apple Developer Program team requirements, and thorough testing before distribution.
- Refreshed AS-15 release-artifact evidence to the 2026-05-28 Apple-source baseline, so signed artifact closure now explicitly covers upload role requirements, Xcode 14+ upload expectations, build string uniqueness, compressed/essential/additional content size metadata, app thinning and DRM/recompression caveats, maximum Mach-O file size limits, Xcode 14+ dSYM download limitations, delivery identifiers/warnings/errors, ExportOptions evidence, and the fact that the local unsigned `CODE_SIGNING_ALLOWED=NO` bundle cannot be reused as the submission artifact.
- Added an Apple-source baseline to AS-15 content-rights evidence, dated 2026-05-27, so IP/content closure now explicitly covers accurate non-misleading metadata/screenshots, rights to all icon/screenshot/preview materials, fictional account data, owned or licensed app content, protected third-party material restrictions, submitting-entity IP rights, third-party service terms permission, authorization availability on request, no false association/sponsorship/endorsement, no copycat or misleading names/icons/previews/metadata, and App Review notes for rights or permission context.
- Refreshed AS-15 content-rights evidence to the 2026-05-28 Apple-source baseline, so IP/content closure now explicitly covers App Store metadata freshness, Notes for Review specificity, screenshots showing the app in use, app previews using app screen captures, accurate unique names/keywords without trademark stuffing, 30-character app-name limits, all-audience metadata, Apple-platform-focused metadata, App Review authorization attachments, local app-icon/screenshot inventory hash checks, no local preview videos, and a local scan for non-Apple platform or marketplace references in iOS metadata.
- Added an Apple-source baseline to AS-12 observability evidence, dated 2026-05-27, so crash/monitoring closure now explicitly covers Xcode Crashes organizer coverage for TestFlight/App Store builds, TestFlight automatic crash-log sharing, App Store opt-in crash sharing, crash refresh by app version/build, two-week top-crash windows and possible one-day delay, app/version/build-string crash selection, required symbolicated crash reports, dSYM and `dwarfdump` UUID matching, automatic symbolication when symbols are uploaded, retaining archives when symbols are not uploaded, App Store Connect dSYM availability when generated, TestFlight feedback visibility, missing crash reports for unresponsive feedback, and build metadata inspection.
- Refreshed AS-12 observability evidence to the 2026-05-28 Apple-source baseline, so crash/monitoring closure now explicitly covers App Store/TestFlight/device crash-log acquisition, Apple crash reports before third-party reports, local dSYM availability when upload symbols are missing, mandatory Xcode archive retention, Xcode 14+ dSYM download limitations, TestFlight 2.3 feedback content, required feedback-viewing roles, crash-feedback filters and downloads, 120-day crash-report availability, Complete-build warning review, Processing-over-24h escalation, and the current absence of local signed IPA/archive evidence.
- Refreshed AS-16 review-access evidence to the 2026-05-28 Apple-source baseline, so App Review access closure now explicitly covers required review-detail API fields, demo-account requirement selection, SSO demo account handling, Notes field 4000-byte limit and language flexibility, required metadata/build selection before submission, Account Holder/Admin/App Manager submission role, Add for Review versus Submit for Review behavior, App Review unresolved-issue replies with attachments, partnership/authorization attachments, local note-byte verification, and the absence of committed review credentials or phone files.
- Refreshed the final signoff record to the 2026-05-28 Apple-source baseline, so final submission closure now explicitly covers platform-scoped submissions, one app-version submission per platform, the two-submission maximum with separate item submissions, platform item separation, draft submission membership, all submitted items accepted before completion, Ready for Review versus Submit for Review, Waiting for Review edit limits, In Review cancellation, Unresolved Issues handling, Developer Rejected retry behavior, Ready for Distribution agreement dependency, and recording the final App Store Connect submission status/items/draft reference.
- Fixed Release-only Swift compilation failures caused by preview/debug factories leaking into Release builds.
- Confined the iOS development-auth bypass to DEBUG builds, added source hygiene coverage that fails if development auth hooks leave `#if DEBUG` source blocks, and added a Release binary scan that fails if development auth credentials or debug launch hooks such as `dev-refresh-token`, `dev-token-mock`, `dev@example.com`, `Dev User`, `wakeve-debug-authenticated`, or `WAKEVE_DEBUG_AUTHENTICATED` are present.
- Updated `iosApp/src/Services/RepositoryProvider.swift` so Release sync traffic uses `https://api.wakeve.app` instead of `http://localhost:8080`.
- Wired the visible privacy policy and terms buttons in `iosApp/src/Views/Auth/LoginView.swift` to public URLs.
- Corrected the iOS Fastlane project/scheme/IPA paths and disabled automatic App Review submission in `fastlane/Fastfile`.
- Added the expected "Data Collection" wording to `docs/PRIVACY_POLICY.md` so the store metadata linter recognizes the privacy policy section.
- Added `Gemfile` and `Gemfile.lock` with Fastlane `2.228.0`, pinned because the local Ruby is `2.6.10` and newer Fastlane releases require Ruby 3.
- Added 1024 x 1024 RGB app icons for default, dark, and tinted appearances in `iosApp/src/Assets.xcassets/AppIcon.appiconset/`.
- Made Fastlane file paths resolve from the repository root so lanes work when launched from the `fastlane/` directory.
- Added `iosApp/src/Wakeve.entitlements` and configured `CODE_SIGN_ENTITLEMENTS`, with `aps-environment=development` for Debug and `aps-environment=production` for Release.
- Added one real iPhone Pro Max screenshot and one real iPad Air 13-inch screenshot per iOS locale in `composeApp/metadata/ios/<locale>/screenshots/`.
- Extended `scripts/lint-store-metadata.sh` with iOS screenshot validation and `--ios-only` / `--android-only` platform filtering.
- Added `docs/APP_STORE_PRIVACY_LABELS.md` as a draft of App Store Connect privacy label answers.
- Added public SvelteKit pages for `/privacy` and `/terms`, matching the URLs opened by the iOS app.
- Added linter coverage for the public `/privacy`, `/terms`, and `/support` web routes, requiring the expected App Store legal/support content, contact addresses, metadata URL mapping, and no unsupported in-app account deletion promise before the verified deletion flow exists.
- Fixed existing Svelte/XState TypeScript diagnostics so the web app now passes `pnpm check`.
- Added App Store Connect URL metadata files for `privacy_url.txt` and `support_url.txt` in `en-US` and `fr-FR`.
- Added iOS `promotional_text.txt` metadata and root `copyright.txt`.
- Extended metadata linting and Fastlane validation to require iOS privacy/support URLs and copyright.
- Added a public SvelteKit `/support` route for the App Store support URL.
- Added `ITSAppUsesNonExemptEncryption=false` to `iosApp/src/Info.plist` for standard export compliance flow.
- Removed unused/high-friction iOS plist declarations: Always location permission, background fetch mode, and legacy armv7 device capability.
- Changed KMP iOS sync/auth defaults from localhost to `https://api.wakeve.app`, so unconfigured shared iOS code no longer falls back to a local backend.
- Added `composeApp/metadata/ios/app_rating_config.json` with a no-restricted-content age rating draft.
- Extended the iOS metadata linter to validate `app_rating_config.json` against Fastlane's current age rating declaration keys, value enums, boolean fields, optional kids age band, and deprecated-key rejection.
- Added `docs/APP_STORE_AGE_RATING.md` and linter coverage for semantic consistency between the age rating JSON, App Store Connect field map, and current product evidence.
- Updated the age-rating evidence and linter coverage to account for Apple's 2026 updated age-rating questionnaire before final App Store Connect submission.
- Added an Apple-source baseline to the App Store age-rating draft, dated 2026-05-27, so age-rating closure now explicitly covers the required age-rating property, App Store Connect questionnaire, content descriptors/in-app controls/capabilities, global and region-specific ratings, OS 26 rating differences, Unrated App Store prohibition, January 31 2026 updated-question deadline, unrestricted web access meaning, updated 4+ capability behavior, Kids category constraints, higher-rating override behavior, and Korea GRAC override handling.
- Added App Review notes plus contact name/email under `composeApp/metadata/ios/review_information/`.
- Added a store-linter guard requiring App Review notes to explain guest/demo review access when no demo credentials are committed, and failing if a demo password file is added to repository metadata.
- Added `composeApp/screenshots/ios/<locale>/` so Fastlane `deliver` can upload screenshots using its expected `screenshots_path`.
- Updated `fastlane/Fastfile` so `upload_appstore` provides `screenshots_path`, uploads screenshots, and passes `app_rating_config_path`.
- Extended the iOS metadata linter to check App Store-sensitive plist declarations, source privacy manifest data types/tracking settings, bundled `PrivacyInfo.xcprivacy`, Release binary strings for localhost, loopback, or cleartext HTTP, and IDFA/App Tracking Transparency consistency with the no-tracking privacy labels.
- Added a local `bundle exec fastlane ios preflight` lane that validates iOS metadata, rebuilds Release for generic iOS without signing, runs the iOS metadata/binary linter, audits web dependencies at low severity, verifies the web app with pinned `npx --yes pnpm@10 check` plus `npx --yes pnpm@10 build`, then runs `scripts/app-store-local-web-route-check.sh` against a local Vite server.
- Added iOS SDK upload-minimum validation to the store linter. Current local result: Xcode 26.5 / iOS SDK 26.5.
- Added optional live URL validation via `./scripts/lint-store-metadata.sh --ios-only --check-live-urls`, so public App Store privacy/support URL reachability, the iOS terms URL, both live AASA endpoints, and backend health can be gated explicitly once DNS is configured.
- Strengthened live AASA validation so it checks reachability, JSON validity, content type, Bundle ID, full Apple Team ID app ID when `APPLE_TEAM_ID` is set, and `/event/`, `/poll/`, `/meeting/`, plus `/invite/` link groups.
- Added `docs/APP_STORE_LIVE_URL_AASA_EVIDENCE.md` and linter/final-audit coverage so AS-14 requires a production deployment evidence record with legal/support URL, third-party notices, AASA, backend health, DNS, cache, rollout, rollback, command-output, reviewer, and date evidence before final signoff.
- Added App Store Connect field-map coverage for `docs/APP_STORE_LIVE_URL_AASA_EVIDENCE.md`, so the manual App Store Connect entry/review flow cannot omit the AS-14 production URL and AASA evidence record.
- Registered the `wakeve://` custom URL scheme in `iosApp/src/Info.plist` and added source plus built-bundle linter checks for it.
- Fixed the app target build settings so the generated Release `Info.plist` includes `CFBundleShortVersionString=1.0` and `CFBundleVersion=1`. The linter now verifies the built app's Bundle ID, display name, marketing version, build version, and URL scheme.
- Added `NSSiriUsageDescription` and `com.apple.developer.siri=true` because the iOS code uses `INPreferences.requestSiriAuthorization` and declares Siri intents.
- Extended the linter to validate source entitlements, APNs environment wiring, Siri usage text, and Siri entitlement presence.
- Extended the linter to validate `NSSpeechRecognitionUsageDescription`, `NSLocationWhenInUseUsageDescription`, `NSPhotoLibraryUsageDescription`, and `NSCameraUsageDescription` when the corresponding iOS APIs are present.
- Extended the linter to validate `NSCalendarsUsageDescription`, `NSPhotoLibraryAddUsageDescription`, `UIBackgroundModes=remote-notification`, and DEBUG-only local API URLs with `https://api.wakeve.app` production fallbacks when the corresponding iOS APIs/configuration are present.
- Added `com.apple.developer.associated-domains` with `applinks:wakeve.app` because `DeepLinkService` already parses `https://wakeve.app` Universal Links.
- Added SvelteKit AASA endpoints at `/.well-known/apple-app-site-association` and `/apple-app-site-association`. They derive the App ID from a real `APPLE_TEAM_ID` or `TEAM_ID` plus bundle ID `com.guyghost.wakeve`, reject invalid Team IDs and the documented `ABCDE12345` placeholder, and declare `/event/*`, `/poll/*`, `/meeting/*`, and `/invite/*`.
- Extended the linter to verify repo-side Universal Links configuration and to check both live AASA URLs when `--check-live-urls` is used.
- Added `com.apple.developer.applesignin` with `Default` because iOS/shared auth code includes Google login paths and the iOS login view exposes native Sign in with Apple.
- Extended the linter to fail if third-party iOS login is present without Sign in with Apple UI/API usage and the matching entitlement.
- Added Fastlane environment checks so signed build/upload lanes fail fast unless required Apple variables are configured: `TEAM_ID` for `ios build`, and `APPLE_ID`, `ITC_TEAM_ID`, `TEAM_ID` for TestFlight/App Store uploads.
- Made `bundle exec fastlane ios upload_testflight` run the local App Store preflight before building or uploading the signed IPA, with linter coverage that asserts the preflight remains before `build` in the lane.
- Strengthened Fastlane and the final submission audit so supplied Apple release variables must have valid formats: `APPLE_ID` as an email-shaped Apple account, numeric `ITC_TEAM_ID`, 10-character uppercase `TEAM_ID` / `APPLE_TEAM_ID`, and matching signing/AASA team IDs for the final App Store release.
- Added an explicit DSA evidence marker, `APP_STORE_DSA_TRADER_STATUS_EVIDENCE_COMPLETE`, so `APP_STORE_DSA_TRADER_STATUS_CONFIRMED=true` cannot satisfy the final audit unless `docs/APP_STORE_DSA_TRADER_STATUS.md` records the App Store Connect DSA decision evidence.
- Added `bundle exec fastlane ios validate_ipa_entitlements` and wired the same check into `bundle exec fastlane ios build`, so a signed IPA must prove `application-identifier`, Team ID, `aps-environment=production`, Siri, Sign in with Apple, and `applinks:wakeve.app` before TestFlight or App Store upload.
- Added `docs/APP_STORE_CAPABILITIES_EVIDENCE.md` and final audit coverage so `APP_STORE_CAPABILITIES_CONFIRMED=true` also requires `APP_STORE_CAPABILITIES_EVIDENCE_COMPLETE=true` for the signed review build and Apple Developer capability/profile evidence.
- Added local unsigned capability-scan evidence to `docs/APP_STORE_CAPABILITIES_EVIDENCE.md`, covering source entitlements, Release build settings, the unsigned `codesign` limitation, and linter checks that keep this separate from the final signed IPA/App ID/provisioning-profile proof.
- Added local account-access gate evidence to `docs/APP_STORE_ACCOUNT_ACCESS_EVIDENCE.md`, covering the intentionally rejected Apple release placeholders and the Fastlane/final-audit gates that require real Apple ID, App Store Connect team ID, Apple Developer Team ID, and matching AASA Team ID before TestFlight or App Store submission.
- Added `docs/APP_STORE_FINAL_SIGNOFF.md` and made `bundle exec fastlane ios upload_appstore` require `APP_STORE_FINAL_SIGNOFF_COMPLETE=true` there after the manual signoff environment variables are set.
- Added a blocker evidence matrix to `docs/APP_STORE_FINAL_SIGNOFF.md` and linter coverage requiring AS-01 through AS-22 plus the final App Store evidence commands before the final signoff record can be considered complete.
- Strengthened `scripts/app-store-submission-audit.sh` so the final non-uploading audit also validates the final signoff blocker evidence matrix, independent of Fastlane preflight.
- Added `bundle exec fastlane ios submission_ready` as a final non-uploading gate, and made `upload_appstore` run strict/live preflight before building and delivering to App Store Connect.
- Made `upload_appstore` run `./scripts/app-store-submission-audit.sh --check-live-urls --run-submission-ready`, so the actual upload path is blocked by the same aggregated final audit used for release signoff.
- Added guards so the documented `+15551234567` placeholder phone cannot pass `submission_ready`, `upload_appstore`, or the final non-uploading audit; those gates require a real reachable App Review phone.
- Added Fastlane, final audit, CI, and linter coverage so the documented Apple release placeholders from `.env.appstore.example` (`release@example.com`, `123456789`, `ABCDE12345`) cannot pass TestFlight or final App Store gates.
- Strengthened Fastlane final signoff validation so `upload_appstore` rejects `docs/APP_STORE_FINAL_SIGNOFF.md` if `APP_STORE_FINAL_SIGNOFF_COMPLETE=true` is forced while unresolved `TBD`, `Pending`, `Status: NOT`, or unchecked checklist items remain.
- Made `bundle exec fastlane ios upload_appstore` fail fast unless the final manual signoff variables are truthy, so the upload path cannot bypass the submission audit's privacy, accessibility, availability, DSA trader status, TestFlight, and Apple capability confirmations.
- Extended the linter to make the Fastlane App Store upload path assert final signoff enforcement, DSA trader status confirmation, strict/live preflight, screenshot upload, age rating config upload, `submit_for_review: false`, and `automatic_release: false`.
- Extended the linter to validate Xcode Release build settings used for App Store builds, including Bundle ID, product name, Info.plist, entitlements, APNs production environment, marketing/build versions, iPhoneOS 26 SDK, deployment target, iPhone/iPad device family, app icon asset, and documented Mac/Vision Pro availability.
- Extended the linter to validate the pinned Fastlane toolchain in `Gemfile` / `Gemfile.lock` and the Appfile's iOS Bundle ID, Android package name, and Apple environment variable wiring.
- Made strict iOS Fastlane preflight require a real App Review phone before launching the Release build, either from `APP_REVIEW_PHONE_NUMBER` or from the `composeApp/metadata/ios/review_information/phone_number.txt` fallback.
- Added `.env.appstore.example` as the non-secret template for required App Store release variables, and updated `.gitignore` so filled `.env` files stay out of version control.
- Extended `.env.appstore.example` with the evidence document and marker required before each manual signoff, including review-access and export-compliance evidence required by final signoff.
- Added linter coverage for `.env.appstore.example` and `.gitignore`, requiring safe placeholder values, false-by-default final signoff flags, PII/secrets guidance, ignored filled `.env.appstore` files, and a trackable `.env.appstore.example`.
- Added matching Fastlane, store-linter, and final-audit validation for App Review phone format: 4-20 digits, common separators allowed, optional leading `+`.
- Wired Fastlane `deliver` to pass App Review contact information explicitly, using `APP_REVIEW_PHONE_NUMBER` for the phone value so personally identifiable contact data can stay in release secrets instead of repository metadata.
- Updated `.github/workflows/store-readiness.yml` so CI runs the current App Store preflight on macOS, syntax-checks the store/audit scripts, verifies strict local preflight behavior, and no longer claims the app is submission-ready when only local gates have passed.
- Strengthened the Store Readiness Summary output and linter coverage so CI explicitly lists the remaining external/manual App Store blockers after local critical gates pass.
- Strengthened `.github/workflows/store-readiness.yml` so the strict preflight check recognizes both expected preparation blockers: missing App Review phone and incomplete final signoff evidence. CI also runs the final audit in expected-failure mode and asserts it still reports `NOT READY` unless local preflight, live URL validation, and the signed final gate are run.
- Replaced the workflow's older hand-written metadata checks with `./scripts/lint-store-metadata.sh`, so CI and local store metadata validation use the same rules.
- Added metadata-linter coverage requiring the store readiness workflow to keep pnpm 10, frozen web dependency installs, `pnpm audit --audit-level low`, and the Fastlane preflight to use pinned `npx --yes pnpm@10` web commands.
- Added CI coverage for `scripts/app-store-local-web-route-check.sh`, so Store Readiness starts a local Vite server with pinned `npx --yes pnpm@10`, `CI=true`, and `APPLE_TEAM_ID=A1B2C3D4E5`, then verifies the public legal pages plus both AASA endpoints before running App Store preflight.
- Added CI validation for the two active App Store OpenSpec proposals, `add-in-app-account-deletion` and `add-ugc-moderation-controls`, and for their required links from `docs/APP_STORE_BLOCKER_REGISTER.md`.
- Added `scripts/app-store-submission-audit.sh` as an aggregated non-uploading final audit for local preflight, live endpoints, Apple variables, App Review phone, signing gate, and manual privacy/accessibility/availability/TestFlight/capability signoffs.
- Strengthened `scripts/app-store-submission-audit.sh` so skipped live URL/AASA validation and skipped `submission_ready` are blockers, not warnings; skipped local preflight cannot produce a ready result. The audit cannot report ready unless those final gates have actually run.
- Added `docs/APP_STORE_SUBMISSION_RUNBOOK.md` with the required Apple Developer setup, production URL checks, App Store Connect fields, and command sequence.
- Strengthened `docs/APP_STORE_SUBMISSION_RUNBOOK.md` so its Current Gate lists every active external, product-gated, and manual App Store blocker, and added linter coverage for the runbook sections, blocker summary, and critical release commands.
- Added `docs/APP_STORE_CONNECT_FIELD_MAP.md` to map App Store Connect fields back to repo-owned metadata, privacy, review, capability, and availability sources.
- Added an Apple-source baseline to the App Store Connect field map, dated 2026-05-27, so manual field entry now explicitly covers App Information shared properties, name/subtitle limits, required Privacy Policy URL, Bundle ID immutability, SKU immutability, required app-level age rating, standard/custom EULA handling, primary language fallback, category matching, app-level privacy answers, screenshot/app-preview versioning, required metadata/build selection, required App Store Connect role, Add for Review versus Submit for Review, App Review information context, and per-platform app-version submissions.
- Added linter coverage for `docs/APP_STORE_CONNECT_FIELD_MAP.md`, requiring the App Information, Version Information, App Review Information, Privacy and Compliance, Capabilities and Availability, and Final Review Checks sections plus the critical App Store Connect fields, repository sources, ordered TestFlight/signoff/final-audit/upload sequence, and final audit/upload commands. The linter now checks the line order of that sequence, not only phrase presence.
- Added `docs/APP_STORE_APP_INFORMATION_EVIDENCE.md` so App Store Connect-only App Information values (SKU, primary language, category, generated age rating) must be recorded before final signoff, and wired that evidence into the final audit and forced-signoff CI guard.
- Added local App Information scan evidence covering the Fastlane Appfile identifier, Xcode Bundle ID/product name, source Info.plist display/version values, EN/FR metadata names and URLs, copyright, age-rating JSON parse result, and local Fastlane metadata validation; the evidence remains incomplete until the live App Store Connect app record is reviewed.
- Refreshed App Store Connect app information evidence to the 2026-05-28 Apple-source baseline, so closure now explicitly covers required app-record roles and signed agreements, app name/subtitle length limits, required privacy policy URL, immutable SKU format, Bundle ID/Xcode matching, localization fallback behavior, user-access choice, and the local `public.app-category.productivity` category evidence.
- Added local Content Rights scan evidence covering app-icon and screenshot inventories, App Store media hashes, absence of preview videos, provider/reference scan output, and the remaining ownership/licensing/provenance evidence required before final signoff.
- Removed unnecessary platform trademark/design-system references from the English and French App Store descriptions, and added linter coverage to prevent those phrases from returning.
- Added `docs/APP_STORE_VERSIONING_EVIDENCE.md` so the release owner must compare the candidate `MARKETING_VERSION` / `CFBundleVersion` with the latest App Store Connect build number before final signoff; wired that evidence into the final audit, final signoff matrix, launch checklist, runbook, field map, environment template comments, and forced-signoff CI guard.
- Added `docs/APP_STORE_OBSERVABILITY_EVIDENCE.md` so `TESTFLIGHT_SMOKE_PASSED=true` also requires crash dashboard, dSYM/symbolication, backend monitoring, Universal Links/AASA monitoring, support mailbox, and privacy-label alignment evidence for the uploaded review build.
- Added local unsigned dSYM scan evidence to `docs/APP_STORE_OBSERVABILITY_EVIDENCE.md`, covering the `Wakeve.app.dSYM` path, matching app/dSYM UUID `C313623B-D8A9-37C3-BA95-7EDFA597FB01`, and the fact that this does not replace uploaded TestFlight/App Review observability.
- Added `docs/APP_STORE_LAUNCH_CHECKLIST.md` for TestFlight smoke testing, monitoring, App Review handoff, and rollback.
- Added linter coverage for `docs/APP_STORE_LAUNCH_CHECKLIST.md`, requiring entry gates, iPhone and iPad TestFlight smoke tests, Universal Links, account deletion, user-generated content moderation, payment compliance, monitoring, App Review submission checks, rollback steps, and exit criteria before `TESTFLIGHT_SMOKE_PASSED=true` can be treated as supported evidence.
- Added `docs/APP_STORE_TESTFLIGHT_EVIDENCE.md` and final audit coverage so `TESTFLIGHT_SMOKE_PASSED=true` also requires `TESTFLIGHT_SMOKE_EVIDENCE_COMPLETE=true` for the uploaded review build.
- Strengthened copied release commands in `docs/APP_STORE_LAUNCH_CHECKLIST.md` and `docs/APP_STORE_READINESS.md` so final strict gates include `APP_REVIEW_PHONE_NUMBER`, and added linter coverage for that App Review phone variable in the launch checklist submission command.
- Marked the older cross-store `docs/store-readiness-checklist.md` and `docs/store-readiness-gaps.md` as superseded for current iOS App Store work because they contained stale iOS assumptions.
- Updated iOS App Store descriptions so privacy wording no longer claims all data stays only on-device while encrypted sync and privacy labels declare collected data.
- Added a metadata linter check to prevent reintroducing an on-device-only privacy claim into iOS App Store descriptions.
- Added `docs/APP_STORE_ACCESSIBILITY_LABELS.md` with conservative App Store Accessibility Nutrition Label draft answers.
- Added linter coverage that validates the Accessibility Nutrition Labels draft has per-device sections, covers all expected label features, and keeps conservative "do not claim yet" guards until TestFlight/device evidence exists.
- Added an Apple-source baseline to the App Store Accessibility Nutrition Labels draft, dated 2026-05-27, so label publication now explicitly covers per-device product-page display, OS 26 visibility, future submission requirement risk, "support not yet indicated" behavior, evaluation criteria, common-task testing matrices, draft/publish flow, inability to unpublish, updateability, accessibility URL behavior, misleading-label App Review risk, response accuracy, and Mac/Vision Pro availability alignment.
- Added `docs/APP_STORE_ACCESSIBILITY_EVIDENCE.md` and final audit coverage so `APP_STORE_ACCESSIBILITY_SIGNOFF=true` also requires `APP_STORE_ACCESSIBILITY_EVIDENCE_COMPLETE=true` for unpublished-label confirmation or current device-backed Accessibility Nutrition Label claims.
- Added `docs/APP_STORE_AVAILABILITY_DECISIONS.md` to capture iPhone/iPad, Mac Apple silicon, and Apple Vision Pro availability decisions that must be confirmed in App Store Connect.
- Added an Apple-source baseline to the App Store availability decisions draft, dated 2026-05-27, so availability closure now explicitly covers required App Store availability before review, 175-country/region availability, Pricing and Availability workflow, Apple Account storefront behavior, all/specific/pre-order choices, country removal effects, 24-hour visibility timing, default Mac Apple silicon and Apple Vision Pro availability, same-runtime compatibility assumptions, app-level device-family choices, required App Store Connect roles, Mac compatibility verification, uploaded-build prerequisite, Kids/visionOS restriction, and launch-country evidence.
- Added `docs/APP_STORE_AVAILABILITY_EVIDENCE.md` and final audit coverage so `APP_STORE_AVAILABILITY_CONFIRMED=true` also requires `APP_STORE_AVAILABILITY_EVIDENCE_COMPLETE=true` for the App Store Connect device-family availability decision.
- Added `docs/APP_STORE_EXPORT_COMPLIANCE_EVIDENCE.md` and final audit/linter coverage so `APP_STORE_FINAL_SIGNOFF_COMPLETE=true` also requires `APP_STORE_EXPORT_COMPLIANCE_EVIDENCE_COMPLETE=true` for the uploaded App Store Connect build.
- Added `docs/APP_STORE_REVIEW_ACCESS_EVIDENCE.md` and final audit/linter coverage so `APP_STORE_FINAL_SIGNOFF_COMPLETE=true` also requires `APP_STORE_REVIEW_ACCESS_EVIDENCE_COMPLETE=true` for the uploaded App Store Connect review build and guest/demo reviewer access path.
- Strengthened the final audit so review-access and export-compliance evidence are also required as soon as all final manual signoff environment variables are forced true, even before `docs/APP_STORE_FINAL_SIGNOFF.md` is complete.
- Added AS-16 to the App Store blocker register and final signoff matrix so the App Review contact phone blocker is explicitly tracked instead of only appearing as an extra audit count when `APP_REVIEW_PHONE_NUMBER` is missing.
- Added AS-17 and `docs/APP_STORE_PRICING_AVAILABILITY_EVIDENCE.md` so App Store Connect Pricing and Availability decisions for price, storefronts, pre-order, education/business distribution, custom apps, tax category, and Paid Apps Agreement compatibility are required before final upload.
- Added local free/no-IAP scan evidence for AS-17, covering StoreKit/IAP source-pattern `NO_MATCHES`, payment-policy and App Review notes stating no premium feature, subscription, digital-content, or paid app sale, SHA-256 hashes for the payment evidence/policy/notes files, and the first-release recommendation to configure Wakeve as a free app with no In-App Purchase products unless App Store Connect or the business owner chooses a paid configuration.
- Added `docs/APP_STORE_DSA_TRADER_STATUS.md` and wired `APP_STORE_DSA_TRADER_STATUS_CONFIRMED` into the final audit plus App Store upload lane, because Apple requires trader status handling for EU distribution.
- Updated DSA and availability docs to use current App Store Connect Help guidance, including trader/non-trader/EU-disabled evidence paths, Mac Apple silicon availability, Apple Vision Pro availability, screenshot/export evidence, and linter coverage for those release decisions.
- Strengthened screenshot linting so both metadata screenshots and Fastlane upload screenshots must use App Store accepted iPhone or iPad dimensions.
- Updated the web lockfile to remove the high-severity `devalue` advisory and the moderate Svelte/SvelteKit/PostCSS advisories.
- Added a targeted pnpm override for `cookie@<0.7.0` to `0.7.2`, clearing the remaining low-severity SvelteKit transitive `cookie` advisory while keeping `pnpm check` and `pnpm build` passing.
- Added `apps/landing/pnpm-workspace.yaml` with `allowBuilds.esbuild=true` so pnpm 11 can install and build the web app without an interactive `approve-builds` step.
- Removed low-risk Release Swift warnings from iOS App Store preparation code: unused APNs/deep-link bindings, deprecated badge count API usage, an exhaustive Sign in with Apple error switch, redundant Kotlin bridge casts, unreachable Explore repository catches, an unused verified URL binding, and KMP `Shared` concurrency import warnings in transport planning.
- Linked `AppIntents.framework` in the iOS target so Xcode 26.5 can run App Intents metadata extraction without emitting the previous missing-framework warning while preserving the existing Siri/Intents implementation.
- Added a Fastlane preflight guard that scans the latest Release xcodebuild activity log and fails the App Store preflight if `warning:` or `error:` diagnostics reappear; the metadata linter now asserts this guard remains wired.
- Added active iOS launch-source hygiene to `./scripts/lint-store-metadata.sh --ios-only`, and removed the remaining `TODO`/`FIXME` markers from `iosApp/src` and `shared/src/iosMain` outside generated framework artifacts.
- Added a DEBUG-only `debugLog` helper for iOS, converted raw Swift `print` calls to that helper, and extended the linter to fail if raw `print`/`debugPrint`/`NSLog` calls return to launch source.
- Wrapped the unused `BackgroundTokenRefresh` BGTaskScheduler integration in DEBUG-only compilation and extended the linter plus Release binary scan to fail if unconfigured BackgroundTasks APIs ship without `BGTaskSchedulerPermittedIdentifiers` and `UIBackgroundModes=processing`.
- Made iOS analytics local-only for the first App Store build until a native Firebase iOS bridge is linked and App Store privacy labels are re-reviewed.
- Removed Kotlin source warnings from the shared iOS build by replacing `GlobalScope` launches with the injected state-machine scope, suppressing intentional generic casts, opting into KMP/interop APIs where needed, and reusing a single metrics export `Json` instance.
- Disabled Gradle configuration cache for the Xcode Kotlin framework build phase so `embedAndSignAppleFrameworkForXcode` completes reliably during Release preflight.
- Added an explicit required-variable section to `docs/APP_STORE_FINAL_SIGNOFF.md`, and extended `scripts/lint-store-metadata.sh` so every final signoff variable must be present in the final signoff record, `.env.appstore.example`, `fastlane/Fastfile`, and `scripts/app-store-submission-audit.sh`.
- Added `docs/APP_STORE_REVIEW_GUIDELINE_AUDIT.md` and wired `APP_STORE_UGC_MODERATION_CONFIRMED` through `.env.appstore.example`, `fastlane/Fastfile`, `scripts/app-store-submission-audit.sh`, `docs/APP_STORE_FINAL_SIGNOFF.md`, and the metadata linter because the project contains comments/chat user-generated content.
- Added an Apple-source baseline to the App Store Review Guideline audit, dated 2026-05-27, so guideline closure now explicitly covers App Review scope, UGC filtering/reporting/blocking/contact requirements, app completeness and functional URL requirements, device testing/demo access, accurate metadata and hidden-feature risks, equivalent login options, in-app account deletion, privacy accuracy, digital IAP versus outside-app goods/services payment rules, current SDK requirements including iOS/iPadOS 26 SDK from April 2026, Accessibility Nutrition Labels, EU DSA trader status, and App Review appeal/communication handling.
- Added `docs/APP_STORE_UGC_MODERATION_EVIDENCE.md` so `APP_STORE_UGC_MODERATION_CONFIRMED=true` now also requires review-build evidence for filtering, reporting, blocking, moderator audit, support contact, and reviewer verification.
- Added `APP_STORE_ACCOUNT_DELETION_CONFIRMED` across `.env.appstore.example`, `fastlane/Fastfile`, `scripts/app-store-submission-audit.sh`, `docs/APP_STORE_FINAL_SIGNOFF.md`, `docs/APP_STORE_REVIEW_GUIDELINE_AUDIT.md`, CI, and the metadata linter because Wakeve supports account creation and Apple Guideline 5.1.1(v) requires in-app account deletion.
- Added `docs/APP_STORE_ACCOUNT_DELETION_EVIDENCE.md` so `APP_STORE_ACCOUNT_DELETION_CONFIRMED=true` now also requires review-build evidence for iOS initiation, backend deletion, local cleanup, guest deletion, tests, and Sign in with Apple revocation handling.
- Removed unsupported account-deletion-from-Profile-Settings wording from the public privacy policy, terms document, SvelteKit privacy page, support page, and terms page until the iOS deletion flow is actually implemented and verified.
- Added metadata-linter coverage so public legal/support pages cannot claim Profile Settings or in-app account deletion before a verified iOS/API deletion flow exists.
- Recorded strict OpenSpec validation evidence for `add-in-app-account-deletion` and `add-ugc-moderation-controls` in their App Store evidence files, and added metadata-linter checks that rerun those validations while the product blockers remain open.
- Added direct final-audit validation for the active App Store OpenSpec proposals so `./scripts/app-store-submission-audit.sh` fails if `add-in-app-account-deletion` or `add-ugc-moderation-controls` no longer validates strictly.
- Strengthened the final audit ready-state semantics so `--skip-preflight` remains usable for quick blocker counts but cannot produce a `READY` result; the release-ready audit must run local preflight, live URL/AASA validation, and `submission_ready`.
- Added `docs/APP_STORE_PAYMENT_COMPLIANCE.md` and wired `APP_STORE_PAYMENT_COMPLIANCE_CONFIRMED` through `.env.appstore.example`, `fastlane/Fastfile`, `scripts/app-store-submission-audit.sh`, `docs/APP_STORE_FINAL_SIGNOFF.md`, `docs/APP_STORE_REVIEW_GUIDELINE_AUDIT.md`, CI, the runbook, and the metadata linter because Wakeve has payment pot, settlement, and Tricount handoff surfaces that must be clearly scoped to real-world shared-event expenses.
- Added an Apple-source baseline to AS-11 payment compliance, dated 2026-05-27, so payment closure now explicitly covers App Review Guideline 3.1.1 digital unlock/IAP requirements, 3.1.1(a) external purchase link entitlement limits, 3.1.3 alternative purchase-method restrictions, 3.1.3(b) multiplatform-service access, 3.1.3(d) person-to-person services, 3.1.3(e) goods and services consumed outside the app, StoreKit external purchase entitlement requirements, and App Review note expectations.
- Refreshed AS-11 payment compliance to the 2026-05-28 Apple-source baseline, so first-submission policy now documents United States versus non-US storefront external-purchase-link handling, StoreKit External Purchase Link entitlement storefront limits, Tricount-only payment handoff, and the need for a new App Store payment review before any future external provider expansion.
- Updated App Review notes to explain that payment, settlement, and Tricount screens are for real-world shared event expenses only, and added linter coverage so those notes must mention no digital unlocks plus trusted Tricount link handling while payment surfaces exist.
- Added `docs/APP_STORE_PAYMENT_EVIDENCE.md` and final audit coverage so `APP_STORE_PAYMENT_COMPLIANCE_CONFIRMED=true` also requires `APP_STORE_PAYMENT_EVIDENCE_COMPLETE=true` for the uploaded review build.
- Added AS-18 and `docs/APP_STORE_SDK_PRIVACY_EVIDENCE.md` so third-party SDK inventory, required SDK privacy manifests, SDK signatures, and the Xcode privacy report are required before final upload.
- Added local unsigned Release SDK/privacy scan evidence for AS-18, covering the embedded `Shared.framework`, the single bundled app privacy manifest and hash, absence of listed third-party binary SDK artifacts in the built app, `otool -L` linked-library output, and the fact that `CODE_SIGNING_ALLOWED=NO` leaves SDK signature evidence open until a signed archive exists.
- Added an Apple-source baseline to AS-18 evidence, dated 2026-05-27, so third-party SDK privacy/signature closure now explicitly requires the signed `.xcarchive` or IPA to be checked for listed SDK privacy manifests, binary SDK signatures, and Xcode privacy-report alignment before `APP_STORE_SDK_PRIVACY_EVIDENCE_COMPLETE=true`.
- Refreshed AS-18 SDK privacy/signature evidence to the 2026-05-28 Apple-source baseline, so closure now also covers developer responsibility for third-party SDK code, Xcode's combined privacy report, SDK signature supply-chain validation, any version of listed SDKs, repackaged listed SDKs, expanded listed-SDK source/binary scans, and expected SDK-developer validation for binary dependencies.
- Added AS-19 and `docs/APP_STORE_RELEASE_CONTROL_EVIDENCE.md` so the App Store Connect release option, phased-release decision, release owner, release window, stop/pause criteria, and first-day monitoring plan are required before final upload.
- Added local release-control upload-path evidence for AS-19, covering Fastlane's final signoff requirements, final audit execution, strict live preflight, `deliver` with `submit_for_review: false`, `automatic_release: false`, and manual-submission instructions in the launch checklist/runbook. AS-19 remains open until App Store Connect release option, owners, timing, and monitoring evidence are captured.
- Added an Apple-source baseline to AS-19 evidence, dated 2026-05-27, so release-control closure now explicitly covers manual, automatic, and scheduled App Store version release options, Pending Developer Release, the 24-hour propagation window, App Store Connect API release-request cancellation limits, and phased-release pause/Release to All Users behavior.
- Added AS-20 and `docs/APP_STORE_MEDIA_LOCALIZATION_EVIDENCE.md` so screenshots, app preview decisions, EN/FR metadata, field limits, and product-page consistency are reviewed before final upload.
- Added local media/localization scan evidence for AS-20, covering EN/FR screenshot folder inventory, iPhone/iPad dimensions, screenshot hash, omitted app previews, Fastlane metadata validation, local App Store field lengths, and shared privacy/support URLs. AS-20 remains open until App Store Connect media and localized metadata are compared with the uploaded review build.
- Added an Apple-source baseline to AS-20 evidence, dated 2026-05-27, so media/localization closure now explicitly covers one-to-ten screenshot requirements, iPad screenshot requirements for iPad-capable apps, optional app previews up to three per supported device size and language, 15-to-30-second preview constraints, poster-frame decisions, and localized metadata fallback/meaning-parity checks.
- Refreshed AS-20 media/localization evidence to the 2026-05-28 Apple-source baseline, so closure now explicitly covers current iPhone 6.9-inch and iPad 13-inch accepted screenshot sizes, highest-resolution screenshot scaling behavior, app preview file size/frame rate/extensions, preview localization fallback, eligible App Store Connect statuses for media upload, and the current local screenshot set hash.
- Tightened AS-20 media/localization evidence against the 2026-05-28 Apple-source baseline, so closure now also records that screenshots and app previews communicate the app experience, Media Manager/scaling choices must be documented, approved media changes require a new version when needed, App Store Connect editable status must be recorded, and local Fastlane metadata validation passed on 2026-05-28.
- Added AS-21 and `docs/APP_STORE_LICENSE_NOTICES_EVIDENCE.md` so dependency license inventory, attribution obligations, notice delivery, and prohibited-license review are required before final upload.
- Added `scripts/app-store-license-inventory.sh` and `docs/APP_STORE_LICENSE_INVENTORY_DRAFT.md` so the AS-21 license-notices evidence starts from a reproducible local inventory across Gradle, npm/pnpm, Fastlane, and local LICENSE/NOTICE files. The script now reads npm package metadata, Ruby gemspec licenses through Bundler, Gradle POM licenses when present in the local Gradle cache, optional remote Maven/RubyGems metadata via `--fetch-remote-metadata`, RubyGems archive `LICENSE` files when API metadata is incomplete, full General Public License wording for copyleft-risk detection, and a draft App Store scope classification. The current draft lists 407 dependencies with 0 unknown licenses, 1 copyleft-risk keyword hit for `ch.qos.logback:logback-classic`, and 0 submitted-iOS unknown/copyleft risks because `logback` is currently only referenced from `server/build.gradle.kts`; AS-21 still remains open until signed-build matching, legal review of scoped exceptions, and notice delivery are verified.
- Added `scripts/app-store-third-party-notices.sh`, `docs/APP_STORE_THIRD_PARTY_NOTICES.md`, and the public `/third-party-notices` web route so the AS-21 notice-delivery path can be regenerated from the license inventory, referenced from App Review notes, checked by the metadata linter, and included in live endpoint validation.
- Regenerated the AS-21 local license inventory and public notices artifacts, recorded their line counts plus SHA-256 hashes in `docs/APP_STORE_LICENSE_NOTICES_EVIDENCE.md`, and added linter coverage that verifies those recorded hashes match the generated files.
- Added AS-22 and `docs/APP_STORE_EULA_EVIDENCE.md` so the App Store Connect License Agreement choice, Apple standard EULA or custom EULA decision, and Terms of Service alignment are required before final upload.
- Added local Terms/EULA alignment scan evidence for AS-22, covering the repository Terms of Service document hash, public `/terms` route hash, exposed terms sections, privacy/support URL consistency across iOS locales, and the remaining App Store Connect/legal approval evidence needed before completion.
- Added an Apple-source baseline to AS-22 evidence, dated 2026-05-27, so EULA closure now explicitly covers the App Store Connect License Agreement field, Apple's standard EULA applying in all regions when no custom EULA is provided, custom EULA territory scope, plain-text-only custom EULA entry, stripped/escaped HTML, line-break-only formatting, and localized custom EULA text in the same field.
- Refreshed AS-22 EULA/Terms evidence to the 2026-05-28 Apple-source baseline, so closure now explicitly requires the Apple standard EULA URL/date if the standard option is selected, App Store Connect screenshot/export evidence, standard-vs-custom territory scope, plain-text/no-HTML custom EULA formatting, localized custom text handling, and legal/product owner approval before `APP_STORE_EULA_EVIDENCE_COMPLETE=true`.
- Added linter coverage for `docs/APP_STORE_BLOCKER_REGISTER.md`, requiring blocker rows AS-01 through AS-22, warning AW-01, the current 21-blocker local preflight baseline, the account-deletion and UGC OpenSpec links, final signoff variables, and final audit closure commands.
- Added an Apple-source baseline to the App Store blocker register, dated 2026-05-27, so blocker closure now explicitly covers required metadata/build selection, App Store Connect role, Add for Review versus Submit for Review, final metadata and fully functional URL requirements, device testing, demo access, UGC protections, in-app account deletion, App Privacy accuracy, IAP versus outside-app payment rules, availability setup, EU DSA trader status, current SDK requirements, export/review statuses, unresolved App Review issue handling, and non-deterministic review ordering.
- Refreshed the App Store blocker register to the 2026-05-28 Apple-source baseline, so closure now also requires per-platform submission scope, draft-submission membership, submitted-item acceptance or exclusion, Ready/Waiting/In Review/Unresolved Issues/Developer Rejected handling, live URL/AASA failure accounting, no Unresolved Issues state, and documented removal/cancel/retry decisions before blockers can be closed.
- Added an Apple-source baseline to the final App Store signoff record, dated 2026-05-27, so the last manual marker now explicitly covers App Review submission scope, required metadata and selected build, required submitter role, Add for Review versus Submit for Review, non-deterministic review ordering, App Review information context, final metadata and functional URL requirements, placeholder-content removal, on-device testing, current contact information, demo account or demo mode access, live backend services, and App Store Connect status/build evidence.
- Added an Apple-source baseline to the App Store submission runbook, dated 2026-05-28, so the operational upload sequence now explicitly covers build upload tools, Apple build processing, bundle/version/build association, required metadata and selected build, required submitter role, Build-section verification, Add for Review versus Submit for Review, In Review status, non-deterministic review ordering, App Review information context, per-platform submission, final metadata and functional URL requirements, placeholder-content removal, on-device testing, current review contact information, demo access, and live backend services.
- Added an Apple-source baseline to the App Store launch checklist, dated 2026-05-28, so TestFlight and launch closure now explicitly covers 90-day TestFlight build windows, provisioning-profile application identifiers, internal and external tester limits, external TestFlight App Review, TestFlight install/feedback/update flow, build sessions and crash metrics, screenshot/crash/general feedback, 120-day crash-report availability, missing crash reports for unresponsive crashes, expiring builds to stop testing, public-distribution guideline expectations for beta builds, final metadata and functional URL requirements, on-device testing, current App Review contact information, full review access, and live backend services.
- Added local App Review access scan evidence, covering review notes/contact metadata, absence of committed demo passwords, App Review phone handling through `APP_REVIEW_PHONE_NUMBER`, Fastlane review information mapping, and iPhone DEBUG login evidence.
- Added a release-visible iOS guest access path for App Review and first-run exploration. `LoginView` now exposes `Continue as guest` / `Continuer en invité` outside DEBUG, `AuthStateManager` creates and restores a local-only guest session without backend tokens, and App Review notes now describe the release-visible local-only guest flow.
- Added local unsigned Release artifact scan evidence, covering the built app/dSYM paths, app and dSYM sizes, app/framework/plist/privacy-manifest hashes, matching executable and dSYM UUID `C313623B-D8A9-37C3-BA95-7EDFA597FB01`, built Bundle ID/version/build/export-compliance values, and the fact that the local `CODE_SIGNING_ALLOWED=NO` build has no signed IPA, no `.xcarchive`, and no code signature.
- Strengthened the release-artifact linter so the recorded local unsigned Release SHA-256 values for the app executable, shared framework executable, built `Info.plist`, and built `PrivacyInfo.xcprivacy` must match the current local build artifacts when they are present.

## Remaining blockers

1. Apple Developer signing is not verified.
   - The Xcode project uses `DEVELOPMENT_TEAM = "${TEAM_ID}"`.
   - A signed archive still needs a real Apple Developer Team ID and App Store provisioning profile.
   - Once signing is available, `bundle exec fastlane ios build` will fail if the signed IPA is missing required App Store entitlements.

2. App privacy answers need product/legal validation.
   - The app privacy manifest now declares the locally visible data types, but App Store Connect privacy labels must match the real data collected by the app and production backend.
   - A draft is available in `docs/APP_STORE_PRIVACY_LABELS.md`.
   - Data classes currently declared: name, email address, user ID, device ID, other user content, coarse location, and product interaction.
   - Still review: photos/media if enabled, crash reporting if added, analytics enablement, and whether any location data should be precise instead of coarse.

3. Push notification capability must still be enabled in Apple Developer.
   - The repository now has `aps-environment` entitlements.
   - The App ID and provisioning profile in Apple Developer must include Push Notifications before a signed archive can be trusted.

4. Siri capability must still be enabled in Apple Developer if the Siri/shortcut code remains enabled.
   - The repository now declares `NSSiriUsageDescription` and `com.apple.developer.siri=true`.
   - The App ID and provisioning profile must include the Siri capability before a signed archive can be trusted.

5. Sign in with Apple capability must still be enabled in Apple Developer.
   - The repository now declares `com.apple.developer.applesignin=Default` and the iOS login screen exposes native Sign in with Apple.
   - The App ID and provisioning profile must include Sign in with Apple before a signed archive can be trusted.

6. Associated Domains capability must still be enabled in Apple Developer if Universal Links remain enabled.
   - The repository now declares `applinks:wakeve.app`.
   - The App ID and provisioning profile must include Associated Domains before a signed archive can be trusted.

7. Production legal/support pages and backend health must be deployed and live.
   - The app now opens `https://wakeve.app/privacy` and `https://wakeve.app/terms`.
   - App Store metadata now references `https://wakeve.app/privacy` and `https://wakeve.app/support`.
   - The Release app uses `https://api.wakeve.app`.
   - `curl` and `./scripts/lint-store-metadata.sh --ios-only --check-live-urls` currently fail for the public `wakeve.app` and `api.wakeve.app` URLs from this environment.
   - The web app now contains public `/privacy`, `/terms`, `/support`, and `/third-party-notices` routes, and its SvelteKit production build targets Vercel explicitly through `@sveltejs/adapter-vercel`; the production domains must still be configured before submission.

8. Universal Links need production AASA environment and live-domain validation.
   - `DeepLinkService` can parse `https://wakeve.app/event`, `/poll`, `/meeting`, and `/invite` URLs.
   - The app now registers `wakeve://` for custom-scheme links.
   - The app now declares `applinks:wakeve.app`, and the web app now serves both AASA endpoints when `APPLE_TEAM_ID` or `TEAM_ID` is configured.
   - Production must set the real Apple Team ID, deploy the web app, and pass both `https://wakeve.app/.well-known/apple-app-site-association` and `https://wakeve.app/apple-app-site-association` live validation before Universal Links can be relied on.

9. App Review contact phone is not configured.
   - App Review notes plus contact name/email are present in Fastlane metadata.
   - A real review contact phone number must be provided as `APP_REVIEW_PHONE_NUMBER`, added at `composeApp/metadata/ios/review_information/phone_number.txt` as a fallback, or entered in App Store Connect before submission.
   - Local review-access evidence now shows a release-visible `Continue as guest` / `Continuer en invité` button that creates a local-only guest session without backend tokens.
   - Before submission, a signed TestFlight/App Store Connect review build must still prove that this guest path reaches the reviewable surfaces, or App Store Connect demo credentials must be configured manually and the review notes updated to match that uploaded build.

10. Android screenshots are still missing for the global store linter.
   - This does not block App Store submission, but it keeps `./scripts/lint-store-metadata.sh --strict` non-zero unless `--ios-only` is used.

11. TestFlight smoke testing and launch monitoring are not complete.
   - `docs/APP_STORE_LAUNCH_CHECKLIST.md` now defines the required device smoke tests, monitoring window, App Review handoff, and rollback plan.
   - These require a signed TestFlight build and live production services.

12. Final App Store signoff record is not complete.
   - `docs/APP_STORE_FINAL_SIGNOFF.md` intentionally keeps `APP_STORE_FINAL_SIGNOFF_COMPLETE=false`.
   - Change it to `true` only after all listed evidence is current and recorded.

13. App Store Accessibility Nutrition Labels are not ready to publish.
   - `docs/APP_STORE_ACCESSIBILITY_LABELS.md` provides conservative draft answers.
   - Existing accessibility audits still show unverified VoiceOver, Dynamic Type, contrast, and device-specific support.

14. Mac Apple silicon and Apple Vision Pro availability need an App Store Connect decision.
   - Current Release build settings report `SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD=NO` and `SUPPORTS_XR_DESIGNED_FOR_IPHONE_IPAD=NO`.
   - `docs/APP_STORE_AVAILABILITY_DECISIONS.md` recommends iPhone/iPad-only for the initial release; App Store Connect must still confirm Mac/Vision Pro availability is disabled before final signoff.

15. EU DSA trader status handling is not confirmed.
   - Apple's current upcoming requirements state that apps without trader status are removed from the App Store in the European Union until trader status is provided and verified.
   - `docs/APP_STORE_DSA_TRADER_STATUS.md` now records the required App Store Connect decision: provide and verify trader status for EU distribution, declare non-trader status, or intentionally disable EU storefront availability for the first release.

16. App Store Pricing and Availability is not confirmed.
   - Price, storefront availability, pre-order, education/business distribution, custom app distribution, tax category, and Paid Apps Agreement compatibility must be recorded from App Store Connect before final upload.
   - Local repository evidence now supports a first-release free/no-IAP configuration, but AS-17 remains open until the App Store Connect Pricing and Availability page, storefront list, tax category, and Paid Apps Agreement status are recorded for the submitted version.
   - `docs/APP_STORE_PRICING_AVAILABILITY_EVIDENCE.md` now records the required evidence and cross-checks against payment, DSA, and availability decisions.
   - The final upload path now requires `APP_STORE_PRICING_AVAILABILITY_CONFIRMED=true`.

17. Third-party SDK privacy manifests and signatures are not confirmed.
   - Apple's third-party SDK requirements apply to listed SDKs included in new apps or updates; privacy manifests are required for listed SDKs, and signatures are required when those listed SDKs are used as binary dependencies.
   - Wakeve currently needs signed-archive evidence for embedded frameworks, listed SDK presence or absence, privacy manifests, SDK signatures, and the Xcode privacy report.
   - The current local unsigned Release build contains one embedded app framework, `Shared.framework`, one bundled `Wakeve.app/PrivacyInfo.xcprivacy`, and no Firebase/GoogleSignIn/GoogleUtilities/AppAuth/BoringSSL/OpenSSL/Protobuf/nanopb/Alamofire/Realm/SDWebImage/Lottie/OneSignal/Sentry binary artifacts by file-name inventory, but `Shared.framework` is not signed in the no-code-signing build.
   - `docs/APP_STORE_SDK_PRIVACY_EVIDENCE.md` now records the required evidence and cross-checks against App Store privacy labels and privacy evidence.
   - The final upload path now requires `APP_STORE_SDK_PRIVACY_CONFIRMED=true`.

18. App Store release control is not confirmed.
   - App Store Connect must have an explicit App Store Version Release option for the submitted iOS version.
   - Fastlane keeps `submit_for_review: false` and `automatic_release: false`; the manual App Store Connect release owner, release window, stop/pause criteria, and first-day monitoring plan still need evidence.
   - The local upload path now documents that `upload_appstore` requires final signoffs, runs the final audit, runs strict live preflight, and then uploads without automatic App Review submission or public release.
   - `docs/APP_STORE_RELEASE_CONTROL_EVIDENCE.md` now records the required manual release, scheduled release, or phased-release decision.
   - The final upload path now requires `APP_STORE_RELEASE_CONTROL_CONFIRMED=true`.

19. App Store media and localization evidence is not confirmed.
   - iPhone and iPad screenshots exist locally for `en-US` and `fr-FR`, but App Store Connect media must still be reviewed against the exact uploaded review build.
   - App previews are intentionally omitted for the first release unless a release owner records preview format, duration, poster-frame, locale, and rights evidence.
   - `docs/APP_STORE_MEDIA_LOCALIZATION_EVIDENCE.md` now records screenshot inventory, screenshot accuracy, preview decision, EN/FR metadata parity, field limits, and product-page consistency.
   - The final upload path now requires `APP_STORE_MEDIA_LOCALIZATION_CONFIRMED=true`.
20. License notices evidence is not confirmed.
   - Apple App Review Guideline 5.2 makes ownership/license review a submission concern for app content and third-party material.
   - `docs/APP_STORE_LICENSE_NOTICES_EVIDENCE.md` now records dependency inventory, license compatibility, attribution obligations, notice delivery, prohibited-license review, and signed-build match evidence.
   - The final upload path now requires `APP_STORE_LICENSE_NOTICES_CONFIRMED=true`.

21. App Store EULA evidence is not confirmed.
   - App Store Connect License Agreement must be reviewed as Apple standard EULA or custom EULA for the submitted app record.
   - Local Terms of Service and the public `/terms` route are present and hashed in `docs/APP_STORE_EULA_EVIDENCE.md`, but this does not replace legal owner approval or the App Store Connect License Agreement setting.
   - `docs/APP_STORE_EULA_EVIDENCE.md` now records the EULA choice, Terms of Service alignment, custom EULA territory/localization scope if applicable, and legal approval.
   - The final upload path now requires `APP_STORE_EULA_CONFIRMED=true`.

22. User-generated content moderation readiness is not confirmed.
   - Comments, chat, event text, locations, meals, and budget item planning text are in scope for App Review Guideline 1.2 if these surfaces are enabled for review.
   - Local implementation evidence now covers filtering, report, block/unblock, moderation audit, iOS discoverability, hidden/pending/rejected states, and App Store audit gates.
   - OpenSpec proposal `openspec/changes/add-ugc-moderation-controls/` remains open for App Store evidence and final validation.
   - `docs/APP_STORE_REVIEW_GUIDELINE_AUDIT.md` now records the required filtering, reporting, blocking, published contact information, timely response, and reviewer-evidence checks.
   - `docs/APP_STORE_UGC_MODERATION_EVIDENCE.md` now records the review-build evidence required before the UGC signoff can be trusted.
   - The final upload path requires `APP_STORE_UGC_MODERATION_CONFIRMED=true`, and `./scripts/test-app-store-ugc-gates.sh` proves forced confirmation is rejected while tasks or evidence are incomplete.

23. Account deletion readiness is not confirmed.
   - Wakeve supports email, OAuth, guest sessions, and Sign in with Apple account flows.
   - Local implementation now exposes the iOS Profile -> Data Management -> Delete Account flow, guest Delete Guest Data flow, authenticated backend deletion endpoint, backend deletion orchestration, local cleanup wiring, Sign in with Apple revocation attempt/failure handling, and focused backend/iOS tests.
   - OpenSpec change `openspec/changes/add-in-app-account-deletion/` is now 13/16 tasks; public wording and local implementation evidence are updated, while uploaded-build evidence, App Review notes/final docs, and full final audit remain incomplete.
   - `docs/APP_STORE_REVIEW_GUIDELINE_AUDIT.md` now records the required in-app initiation, deletion scope, completion messaging, guest-account handling, and Sign in with Apple token-revocation evidence.
   - `docs/APP_STORE_ACCOUNT_DELETION_EVIDENCE.md` now records the review-build evidence required before the account deletion signoff can be trusted.
   - The final upload path now requires `APP_STORE_ACCOUNT_DELETION_CONFIRMED=true`.

24. Payment and external purchase compliance is not confirmed.
   - Wakeve has payment pot, settlement, and Tricount handoff surfaces.
   - Apple Guideline 3.1.1 requires in-app purchase for digital feature/content unlocks, while Guideline 3.1.3(e) allows other payment methods for physical goods or services consumed outside the app.
   - `docs/APP_STORE_PAYMENT_COMPLIANCE.md` now records the required evidence that payment/Tricount surfaces are limited to real-world shared-event expenses and do not unlock digital app functionality.
   - The final upload path now requires `APP_STORE_PAYMENT_COMPLIANCE_CONFIRMED=true`.

## Recommended next commands

```bash
# Install the pinned Fastlane toolchain
bundle install

# Local App Store preflight without signing or upload
bundle exec fastlane ios preflight

# Local AASA endpoint smoke test after web build
cd apps/landing && APPLE_TEAM_ID=<APPLE_TEAM_ID> pnpm preview --host 127.0.0.1 --port 4173
curl -i http://127.0.0.1:4173/.well-known/apple-app-site-association
curl -i http://127.0.0.1:4173/apple-app-site-association

# Optional stricter checks once the review phone and live DNS are configured
bundle exec fastlane ios preflight strict:true live_urls:true

# Aggregated final audit before upload
./scripts/app-store-submission-audit.sh --check-live-urls --run-submission-ready

# Final non-uploading gate once Apple credentials, review phone, live URLs, and signing are configured
APPLE_ID=<APPLE_ACCOUNT_EMAIL> ITC_TEAM_ID=<APP_STORE_CONNECT_TEAM_ID> TEAM_ID=<APPLE_TEAM_ID> APPLE_TEAM_ID=<APPLE_TEAM_ID> APP_REVIEW_PHONE_NUMBER=<APP_REVIEW_PHONE_NUMBER> bundle exec fastlane ios submission_ready

# Build a signed archive once TEAM_ID/profiles are configured
TEAM_ID=<APPLE_TEAM_ID> bundle exec fastlane ios build

# Inspect an already-built signed IPA's entitlements
TEAM_ID=<APPLE_TEAM_ID> bundle exec fastlane ios validate_ipa_entitlements ipa:build/ios/WakeveApp.ipa

# Upload to TestFlight first
APPLE_ID=<APPLE_ACCOUNT_EMAIL> ITC_TEAM_ID=<APP_STORE_CONNECT_TEAM_ID> TEAM_ID=<APPLE_TEAM_ID> bundle exec fastlane ios upload_testflight

# Upload IPA, metadata, and screenshots to App Store Connect after final signoffs pass
APP_STORE_PRIVACY_SIGNOFF=true APP_STORE_ACCESSIBILITY_SIGNOFF=true APP_STORE_AVAILABILITY_CONFIRMED=true APP_STORE_DSA_TRADER_STATUS_CONFIRMED=true APP_STORE_PRICING_AVAILABILITY_CONFIRMED=true APP_STORE_SDK_PRIVACY_CONFIRMED=true APP_STORE_RELEASE_CONTROL_CONFIRMED=true APP_STORE_MEDIA_LOCALIZATION_CONFIRMED=true APP_STORE_LICENSE_NOTICES_CONFIRMED=true APP_STORE_EULA_CONFIRMED=true APP_STORE_ACCOUNT_DELETION_CONFIRMED=true APP_STORE_UGC_MODERATION_CONFIRMED=true APP_STORE_PAYMENT_COMPLIANCE_CONFIRMED=true TESTFLIGHT_SMOKE_PASSED=true APP_STORE_CAPABILITIES_CONFIRMED=true APPLE_ID=<APPLE_ACCOUNT_EMAIL> ITC_TEAM_ID=<APP_STORE_CONNECT_TEAM_ID> TEAM_ID=<APPLE_TEAM_ID> APPLE_TEAM_ID=<APPLE_TEAM_ID> APP_REVIEW_PHONE_NUMBER=<APP_REVIEW_PHONE_NUMBER> bundle exec fastlane ios upload_appstore
```

## Apple references

- App privacy details: https://developer.apple.com/app-store/app-privacy-details/
- Submitting apps and current SDK requirements: https://developer.apple.com/app-store/submitting/
- Third-party SDK requirements: https://developer.apple.com/support/third-party-SDK-requirements/
- Select an App Store version release option: https://developer.apple.com/help/app-store-connect/manage-your-apps-availability/select-an-app-store-version-release-option/
- Release a version update in phases: https://developer.apple.com/help/app-store-connect/update-your-app/release-a-version-update-in-phases
- Screenshot specifications: https://developer.apple.com/help/app-store-connect/reference/screenshot-specifications
- App preview specifications: https://developer.apple.com/help/app-store-connect/reference/app-preview-specifications
- Localize app information: https://developer.apple.com/help/app-store-connect/manage-app-information/localize-app-information
- App Store Connect app privacy: https://developer.apple.com/help/app-store-connect/reference/app-information/app-privacy/
- Privacy manifests: https://developer.apple.com/documentation/bundleresources/privacy-manifest-files
- Required-reason APIs: https://developer.apple.com/documentation/BundleResources/describing-use-of-required-reason-api
- Siri authorization: https://developer.apple.com/documentation/sirikit/requesting-authorization-to-use-siri
- Siri entitlement: https://developer.apple.com/documentation/bundleresources/entitlements/com.apple.developer.siri
- Associated Domains entitlement: https://developer.apple.com/documentation/bundleresources/entitlements/com.apple.developer.associated-domains
- Sign in with Apple entitlement: https://developer.apple.com/documentation/bundleresources/entitlements/com.apple.developer.applesignin
- Supporting associated domains: https://developer.apple.com/documentation/xcode/supporting-associated-domains
- Screenshot specifications: https://developer.apple.com/help/app-store-connect/reference/app-information/screenshot-specifications
- Build uploads: https://developer.apple.com/help/app-store-connect/manage-builds/upload-builds
- App pricing and availability: https://developer.apple.com/help/app-store-connect/reference/pricing-and-availability/app-pricing-and-availability
- Set a price: https://developer.apple.com/help/app-store-connect/manage-app-pricing/set-a-price
- Accessibility Nutrition Labels: https://developer.apple.com/help/app-store-connect/manage-app-accessibility/overview-of-accessibility-nutrition-labels/
- Apple Vision Pro availability: https://developer.apple.com/help/app-store-connect/manage-your-apps-availability/manage-availability-of-iphone-and-ipad-apps-on-apple-vision-pro
- DSA trader status requirement: https://developer.apple.com/help/app-store-connect/manage-compliance-information/manage-european-union-digital-services-act-trader-requirements
- App Review Guidelines: https://developer.apple.com/app-store/review/guidelines/
