package com.guyghost.wakeve.presentation.statemachine

/**
 * Tests for MeetingServiceStateMachine.
 *
 * Real tests are in jvmTest/kotlin/.../MeetingServiceStateMachineTest.kt
 * which requires the full database + use case infrastructure (JVM only).
 */
// Tests moved to jvmTest — see MeetingServiceStateMachineTest.kt in jvmTest
