## 1. Model and Review Gate

- [x] 1.1 Model the complete invitation experience in `models/ios-event-invitation-experience.md`, including owners, total states, events, guards, effects, errors, cancellation, retry, permissions, offline behavior, terminal states, and invariants.
- [x] 1.2 @review Review and approve the model against the required Model → Review sequence, including the global read-only policy, secure invitation boundary, separate audience axes, notification axes, and absence of free-text/LLM transitions.
- [x] 1.3 @review Review this proposal, design, and deltas for duplication or conflict with existing specs and active changes.
- [x] 1.4 Obtain explicit user approval for `expand-ios-invitation-experience`. No RED test or production implementation task below may start before this gate is approved.

## 2. RED Test Baseline

- [x] 2.1 @tests Add failing table-driven shared tests for Library projections, total next-action mapping, load cancellation, missing data, stale/conflict state, the `PAST`/`FINALIZED` archive override, and owner rejection of stale `DeleteEvent` for historical events.
- [x] 2.2 @tests Add failing SQLDelight migration/repository tests for aggregate schema/revision, total artwork union, legacy URL migration, sanitized issues, operation receipts, direct-invite batches/outcomes, event notification preferences, mixed old/new/rolled-back writers, writer fencing, full event cascade, asset reference counting/retention, and account erasure with no orphan PII.
- [x] 2.3 @tests Add failing tests for Studio `NEW`/`EDIT_EXISTING`, release-1 `NONE`/preset/owner-authorized-server-asset selection, absence of device picker/upload, current-revision preview, stale callbacks, `UpdateDraftAggregate` authorization/status/base-revision guards, atomicity, idempotent operation replay, cancellation, and post-commit sync retry.
- [x] 2.4 @tests Add failing tests proving invitation delivery, approval, membership, RSVP, and retained-date validation remain independent; counts reconcile with identities; direct-invite retry/cancellation affects only unresolved recipients; release-1 direct invite is DRAFT-only; persisted recipient keys are protected pseudonymous/HMAC identifiers; and retention/event deletion/account erasure remove or anonymize recipient data.
- [x] 2.5 @tests Add failing tests for event/account/system notification axes, effective priority, OS denial, globally disabled types, quiet hours, event-preference writes, exact operation-key matching, cancellation, and pending sync.
- [x] 2.6 @tests Add failing route/deep-link tests for every canvas action, missing route fallback, access denial, and the global `PAST`/`FINALIZED` redirect to Archive before owner callbacks.
- [x] 2.7 @tests Add failing iOS source/UI contracts for all six new surfaces, one primary action, real repository inputs, accessible state, no placeholder routes, and no new-surface use of PassKit, local invitation tokens, local invitation URLs, local QR generation, or client-authorized redeem; do not assert global legacy cleanup owned by `harden-event-invitation-links`.
- [x] 2.8 @tests Add failing localization parity and long-copy tests for en/fr/es/it/pt, coordinated with `standardize-product-language`.

## 3. Shared Domain, Persistence, and Owners

- [x] 3.1 @codegen Implement total shared contracts for temporal classification, interaction policy, Library projections/actions, `PreviousStableState`, Studio states/events, artwork, route capabilities, Audience axes/operations, Information operation keys, and Archive snapshots.
- [x] 3.2 @codegen Add SQLDelight migrations for aggregate schema/revision, artwork/migration issues, operation receipts, protected direct-invite batches/outcomes, and event notification preference without deleting or rewriting legacy source data.
- [x] 3.3 @codegen Implement the one-way artwork migration: validated allowlisted HTTPS URLs become `LEGACY_REMOTE`; missing/invalid values become `NONE`; successful release-1 preset or owner-authorized-server-asset edits become `STRUCTURED`; device picker/upload remains absent.
- [x] 3.4 @codegen Implement repository-backed Library and Archive projections with structured ordering, exact cancellation restore, freshness/sync decoration, and global read-only override.
- [x] 3.5 @codegen Implement Studio repositories and `UpdateDraftAggregate` so organizer, DRAFT status, base revision, total release-1 artwork, incremented revision, operation receipt, and sync operation are revalidated and committed atomically.
- [x] 3.6 @codegen Implement aggregate schema versioning, minimum-writer fencing, field-aware compatibility adapters, revision-checked patch sync, and UI-only rollback that preserves new data, writers, receipts, and guards.
- [x] 3.7 @codegen Extend event cascade deletion, foreign keys/explicit ordering, server-asset reference counting/retention, and authenticated account-erasure inventory to every new table with no orphan record or PII.
- [x] 3.8 @codegen Implement DRAFT-only direct-invite batch persistence through the existing owner capability using stable idempotency, per-recipient outcomes, protected pseudonymous/HMAC keys, encrypted short-lived delivery data where required, retention, unresolved-only retry/cancellation, and coherent Audience snapshots.
- [x] 3.9 @codegen Implement the event-scoped notification preference repository and effective three-axis projection without mutating account settings or requesting system permission.
- [x] 3.10 @codegen Implement typed Information snapshots and operation-specific state around existing guarded Calendar/Maps/Weather, leave/remove/delete, and retry/cancellation owners; reject `PAST`/`FINALIZED` mutations and route historical remediation to the existing support/privacy authority without adding a lifecycle status.
- [x] 3.11 @codegen Keep `SecureShareCapability` hidden/unavailable and `GuestApprovalPolicy.NOT_SUPPORTED` until `harden-event-invitation-links` supplies reviewed matching capabilities; prohibit token/URL/QR fallbacks only in the six new surfaces and leave global legacy cleanup to that change's authoritative cutover.

## 4. iOS Release-1 Surfaces

- [x] 4.1 @codegen Build the repository-backed Event Library with Drafts, Hosting, Attending, Upcoming, and Past projections, Wallet-inspired glanceable hierarchy, native filtering, sync/freshness state, and one next action per card.
- [x] 4.2 @codegen Build Creation Studio for `NEW` and guarded `EDIT_EXISTING` with `NONE`, curated presets, and already-authorized server assets only when their owner exists; include mandatory current-revision preview, validation, commit, pending sync, failure, retry, and cancellation, with no device photo picker/upload.
- [x] 4.3 @codegen Build Audience with one coherent snapshot, explicit invitation/approval/membership/RSVP/date axes, DRAFT-only direct invite batches, per-recipient outcomes, and honest secure-share dependency states.
- [x] 4.4 @codegen Reuse the completed invitation canvas and connect its typed actions to real Poll, Participants, Organization, local detail, and Archive routes without reintroducing business decisions in SwiftUI.
- [x] 4.5 @codegen Build Event Information with structured metadata, typed Calendar/Maps/Weather destinations, three visible notification axes, event-preference editing, permission-gated leave/remove/delete confirmations for interactive events, and no delete action for `PAST`/`FINALIZED` Archive records.
- [x] 4.6 @codegen Build the read-only Archive for `PAST` or `FINALIZED` events, including artwork, settled summaries, freshness/sync warnings, reload, and safe deep-link resolution.
- [x] 4.7 @codegen Apply native iOS chrome, Wakeve branded content hierarchy, iOS 26 Liquid Glass only where appropriate, material/opaque fallbacks, one-action rules, and no PassKit/payment/admission semantics.
- [x] 4.8 @codegen Add en/fr/es/it/pt localization keys and accessibility semantics using canonical concepts from `standardize-product-language`; keep domain identifiers out of user-visible copy.
- [x] 4.9 @docs Document the six-surface architecture, total artwork/migration, owner boundaries, secure-link dependency, notification axes, global read-only routing, rollout, and rollback.

## 5. Verification

- [x] 5.1 @tests Run all new shared model, repository, migration, Studio, Audience, notification, route, access, offline/pending-sync, cancellation, retry, deletion/erasure, and terminal-state suites.
- [x] 5.2 @tests Verify migration from empty, valid legacy, invalid legacy, partially migrated, replayed-operation, old-writer, new-writer, background-sync, and UI-rollback states; confirm protected aggregate fields survive, every event has one total artwork projection, cascade/reference counts are correct, account erasure leaves no orphan PII, and no sensitive URL/recipient diagnostics exist.
- [x] 5.3 @tests Run security/source scans proving the six new surfaces do not use `InvitationTokenCodec`, construct a redeemable URL, generate a local QR, import PassKit for this experience, log/persist an opaque share payload, or enable public share before the bound secure capability is ready; do not treat legacy global code awaiting `harden-event-invitation-links` task 5.2 as this change's cleanup failure.
- [x] 5.4 @tests Build the full KMP project and iOS app, run the complete relevant shared/server/iOS regression suites, validate all five localization catalogs, and run `git diff --check`.
- [ ] 5.5 @tests On a real configured iPhone Simulator, exercise real repository-backed flows—not preview-only fixtures—for Library filters, Studio new/edit/relaunch with release-1 artwork choices, direct-invite outcomes, Detail owner routes, Information writes/providers, Archive override, offline/pending sync, conflict, retry, cancellation, and app restart.
- [x] 5.6 @tests Verify organizer, eligible member, pending/restricted, and non-member access across every lifecycle state; verify stale mutating actions and deep links resolve to Archive under `PAST`/`FINALIZED` before any owner callback, `DeleteEvent` rejects historical records, and support/privacy remediation does not mutate the event.
- [ ] 5.7 @tests Verify all six surfaces with VoiceOver, accessibility Dynamic Type, 44-point targets, light/dark mode, increased contrast, Reduce Motion, Reduce Transparency, compact height, landscape, and long en/fr/es/it/pt strings.
- [x] 5.8 @docs Create and maintain `design-qa.md` with canonical sources, app/build version, device, runtime, native viewport, real-repository capture paths, accessibility captures/results, comparison history, P0/P1/P2 fixes, designer verdict, and the exact terminal line `final result: passed` only after approval.
- [x] 5.9 @designer Compare the implemented real-data simulator flows with every canonical Mobbin reference for hierarchy, crop, information density, navigation familiarity, artwork quality, state clarity, and action priority; record and resolve all P0/P1/P2 findings in `design-qa.md`.
- [x] 5.10 @review Verify implementation against the approved model and release-1 rescope: no second lifecycle owner, no implicit axis propagation, no direct repository mutation from views, no free-text/LLM transition, no permission expansion from stale data, no local photo picker/upload, and no secure-link boundary regression.
- [x] 5.11 @review Verify the completed canvas remains intact as a consumed building block; mixed-version writers and rollback preserve protected data; cascade/erasure are complete; and weather/AI/parity/language changes are integrated without duplicated ownership.
- [x] 5.12 @tests Run `openspec validate expand-ios-invitation-experience --strict`, the full relevant regression matrix, and final source/diff checks.
- [x] 5.13 @review Confirm final QA evidence records real repository flows, accessibility results, Mobbin/in-app comparisons, dependency-disabled secure sharing, rollout/rollback proof, and `final result: passed` before handoff.
